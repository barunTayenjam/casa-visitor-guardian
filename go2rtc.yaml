streams:
  cam1:
    - ${CAM1_RTSP_URL}
  cam2:
    - ${CAM2_RTSP_URL}

api:
  listen: ":1984"
  origin: "*"

webrtc:
  listen: ":8555"
  candidates:
    - ${GO2RTC_ADVERTISED_HOST}:8555
    - stun:stun.l.google.com:19302
    - stun:stun1.l.google.com:19302

log:
  level: "info"
  format: "text"
