---
id: 1.2
wave: 1
autonomous: true
objective: Add preprocessing pipeline — Gaussian blur, morphological operations, contour filtering
requirements:
  - DET-02
  - DET-05
tasks:
  - Add Gaussian blur (kernel size 5x5) before MOG2
  - Add morphological operations (erode/dilate) to clean up contours
  - Filter contours by minimum area (configurable, default 500 pixels)
  - Filter out small contours that represent shadows/noise
  - Add time-of-day sensitivity multiplier
files:
  - server/src/detection/optimizedMotionDetection.ts
  - server/src/config/detectionConfig.ts
---

# Plan 1.2: Preprocessing Pipeline

Add preprocessing pipeline — Gaussian blur, morphological operations, contour filtering.

## Tasks

1. Add Gaussian blur (kernel size 5x5) before MOG2
2. Add morphological operations (erode/dilate) to clean up contours
3. Filter contours by minimum area (configurable, default 500 pixels)
4. Filter out small contours that represent shadows/noise
5. Add time-of-day sensitivity multiplier

## Files

- `server/src/detection/optimizedMotionDetection.ts`
- `server/src/config/detectionConfig.ts`

## Verification

- [ ] Shadows don't trigger events
- [ ] Small movements (insects, leaves) filtered
- [ ] Clean contour data passed to event logic
