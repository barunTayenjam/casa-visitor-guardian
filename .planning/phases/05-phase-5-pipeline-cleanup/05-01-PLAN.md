---
phase: 05-pipeline-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/config/index.ts
  - server/.env
  - docker-compose.yml
  - server/src/detection/cleanupService.ts
autonomous: true
requirements:
  - CLN-01
  - CLN-04

must_haves:
  truths:
    - "Python pipeline is the default operational mode (PIPELINE_MODE defaults to 'python-native')"
    - "Legacy config flags and env vars for Node.js detection removed or deprecated"
    - "HTTP polling and retry logic for detection removed from backend"
    - "Detection events continue to flow normally after the switch"
  artifacts:
    - path: "server/src/config/index.ts"
      provides: "Default PIPELINE_MODE changed from 'legacy' to 'python-native'"
      contains: "PIPELINE_MODE.*python-native"

key_links:
  - from: "server/src/config/index.ts"
    to: "server/src/services/opencvMicroserviceClient.ts"
    via: "detection dispatch"
    pattern: "opencvMicroserviceClient"
  - from: "server/src/services/opencvMicroserviceClient.ts"
    to: "opencv-service/app.py"
    via: "HTTP / WebSocket"
    pattern: "Flask|WebSocket"
---

<objective>
Switch the default detection pipeline from legacy Node.js to Python-native, and remove HTTP polling/retry logic.

The Python-native pipeline (FFmpeg + MOG2 + YOLO + ArcFace + WebSocket)
has been running alongside the legacy pipeline since Phase 1. This plan
makes it the default and removes the HTTP polling fallback.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
Current default in config/index.ts:
```typescript
PIPELINE_MODE: process.env.PIPELINE_MODE || 'legacy'
```

Target:
```typescript
PIPELINE_MODE: process.env.PIPELINE_MODE || 'python-native'
```

HTTP polling/retry lives in `opencvMicroserviceClient.ts` — should be removed or gated behind the env var.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch PIPELINE_MODE default to python-native</name>
  <files>server/src/config/index.ts</files>
  <action>
    Change `PIPELINE_MODE: process.env.PIPELINE_MODE || 'legacy'` to
    `PIPELINE_MODE: process.env.PIPELINE_MODE || 'python-native'`.

    Update any dependent checks that test for `'legacy'` as the default assumption.
  </action>
  <verify>
    <automated>grep -q "PIPELINE_MODE.*python-native" server/src/config/index.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Clean up legacy config flags and env vars</name>
  <files>server/src/config/index.ts</files>
  <action>
    Identify env vars and config flags related to the legacy Node.js detection pipeline:
    - Remove or deprecate flags that only apply to legacy mode
    - Ensure backward compat aliases where necessary
    - Update .env.example if present
  </action>
</task>

<task type="auto">
  <name>Task 3: Remove HTTP polling and retry logic</name>
  <files>server/src/services/opencvMicroserviceClient.ts</files>
  <action>
    Remove or disable:
    - HTTP polling loop for detection frames
    - Retry logic for failed HTTP detection requests
    - Any fallback HTTP endpoints that duplicate WebSocket delivery

    The Python pipeline now delivers detection results via WebSocket, so HTTP
    polling is no longer the primary path.
  </action>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node.js → Config | Changing the default mode affects all deployments. Must ensure python-native path is fully functional. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01 | DoS | Config change | mitigate | Keep `PIPELINE_MODE` env var override for rollback |
</threat_model>

<verification>
1. Config defaults: `grep -q "python-native" server/src/config/index.ts`
2. HTTP polling removed: `! grep -q "polling\|retry" server/src/services/opencvMicroserviceClient.ts`
3. Detection endpoints return same format after switch
</verification>

<success_criteria>
- [ ] PIPELINE_MODE defaults to 'python-native'
- [ ] Legacy env vars cleaned up
- [ ] HTTP polling/retry logic removed from opencvMicroserviceClient
- [ ] Rollback possible via env var override
</success_criteria>
