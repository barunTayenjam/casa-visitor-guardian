---
phase: 07-security-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - server/src/routes/notificationRoutes.ts
  - server/src/routes/motion.ts
  - server/src/routes/detection-operations.ts
  - server/src/routes/highlights.ts
  - server/src/routes/visitorRoutes.ts
  - server/src/routes/event-search.ts
  - server/src/routes/faceConfigRoutes.ts
  - server/src/routes/faceEmbeddingRoutes.ts
  - server/src/routes/detectionRedoRoutes.ts
  - server/src/routes/nvidiaRoutes.ts
  - server/src/utils/auditLogger.ts
  - server/src/config/index.ts
  - .env.example
autonomous: true
requirements:
  - SEC2-04
  - SEC2-05
  - SEC2-06

must_haves:
  truths:
    - "Every route that parses req.body, req.query, or req.params has validate() middleware with explicit schemas"
    - "No (req as any).user patterns remain in notificationRoutes.ts — all use req.user!.userId"
    - "Empty cameras.json logs a warning instead of silently returning []"
    - "Quiet hours timezone defaults to process.env.TZ instead of hardcoded 'Asia/Kolkata'"
    - "Single consolidated .env.example at project root with all required env vars"
  artifacts:
    - path: "server/src/routes/notificationRoutes.ts"
      provides: "validate() on all endpoints, req.user!.userId access, TZ-based timezone"
      not_contains: "(req as any).user"
    - path: "server/src/routes/motion.ts"
      provides: "validate() on GET /events, GET /:cameraId/events, POST /:cameraId/analyze"
      contains: "validate("
    - path: "server/src/routes/highlights.ts"
      provides: "validate() on GET /:date with sort enum constraint"
      contains: "validate("
    - path: "server/src/config/index.ts"
      provides: "Warning log on empty camera config"
      contains: "logger.warn"
  key_links:
    - from: "server/src/routes/notificationRoutes.ts"
      to: "server/src/middleware/validation.ts"
      via: "import { validate } from '../middleware/validation.js'"
      pattern: "import.*validate.*from.*validation"
    - from: "server/src/routes/notificationRoutes.ts"
      to: "server/src/middleware/auth.ts"
      via: "req.user!.userId using Express Request augmentation"
      pattern: "req\\.user!\\.userId"
    - from: "server/src/config/index.ts"
      to: "logger"
      via: "logger.warn() on empty cameras array"
      pattern: "logger\\.warn.*[Cc]amera"
---

<objective>
Apply input validation to all unvalidated route files, fix (req as any).user type-safety bypass, add camera config failure visibility, fix hardcoded timezone, and consolidate env files.

Purpose: 8 route files accept user input without validation schemas, enabling injection attacks. The (req as any).user pattern in notificationRoutes bypasses Express type augmentation. Silent camera config failures hide operational issues. Hardcoded timezone breaks non-IST users.

Output: All routes validated with explicit schemas, typed auth access, visible config failures, proper timezone, single .env.example.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase-7-security-hardening/07-CONTEXT.md
@.planning/phases/07-phase-7-security-hardening/07-RESEARCH.md
@.planning/phases/07-phase-7-security-hardening/REQS.md
@.planning/phases/07-phase-7-security-hardening/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From server/src/middleware/validation.ts:
```typescript
export interface ValidationSchema {
  body?: Record<string, ValidationRule>;
  query?: Record<string, ValidationRule>;
  params?: Record<string, ValidationRule>;
}

export interface ValidationRule {
  type: 'string' | 'number' | 'boolean' | 'email' | 'url' | 'object' | 'array';
  required?: boolean;
  minLength?: number;
  maxLength?: number;
  min?: number;
  max?: number;
  pattern?: RegExp;
  enum?: string[];
  custom?: (value: unknown) => boolean | string;
}

export function validate(schema: ValidationSchema): RequestHandler;
export const commonSchemas: {
  cameraId: { params: { id: { type: 'string'; required: true; pattern: RegExp } } };
  createCamera: { body: { ... } };
  pagination: { query: { page: { type: 'number'; required: false; min: 1 }; pageSize: { type: 'number'; required: false; min: 1; max: 100 } } };
  eventFilter: { query: { ... } };
};
```

From server/src/middleware/auth.ts:
```typescript
// Express Request augmentation (already declared):
declare module 'express-serve-static-core' {
  interface Request {
    user?: JWTPayload;  // { userId: string; username: string; role: string; iat?: number; exp?: number }
  }
}
// Exports: authenticate(), requireUser, requireAdmin, requireViewer, optionalAuth
```

From server/src/routes/auth.ts (GOLD STANDARD pattern to follow):
```typescript
// Pattern: import validate → chain in middleware → specific schema per route
import { validate } from '../middleware/validation.js';
router.post('/register',
  authenticate({ roles: ['admin'] }),
  validate({
    body: {
      username: { type: 'string' as const, required: true, minLength: 3, maxLength: 50, pattern: /^[a-zA-Z0-9_-]+$/ },
      // ...
    }
  }),
  (req, res) => authController.register(req, res)
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix (req as any).user in notificationRoutes + add validation schemas + fix hardcoded timezone + fix auditLogger</name>
  <files>server/src/routes/notificationRoutes.ts, server/src/utils/auditLogger.ts</files>
  <read_first>
    - server/src/routes/notificationRoutes.ts (full file — 279 lines, 9 occurrences of `(req as any).user`)
    - server/src/middleware/validation.ts (lines 1-70 for validate() signature, lines 172-361 for commonSchemas)
    - server/src/routes/auth.ts (full file — gold standard validation pattern)
    - server/src/utils/auditLogger.ts (lines 113-138 for getClientIP, getUserId, getUsername, getSessionId methods)
    - server/src/middleware/auth.ts (lines 1-10 for Request augmentation)
  </read_first>
  <acceptance_criteria>
    - `grep -c '(req as any).user' server/src/routes/notificationRoutes.ts` returns 0
    - `grep 'req.user!.userId' server/src/routes/notificationRoutes.ts` returns 9 matches (one per handler)
    - `grep 'import.*validate.*from.*validation' server/src/routes/notificationRoutes.ts` returns a match
    - `grep 'validate(' server/src/routes/notificationRoutes.ts` returns at least 6 matches (one per validated endpoint)
    - `grep "Asia/Kolkata" server/src/routes/notificationRoutes.ts` returns 0 matches
    - `grep "process.env.TZ" server/src/routes/notificationRoutes.ts` returns matches on lines 181 and 257
    - `grep -c '(req as any).user' server/src/utils/auditLogger.ts` returns 0
    - TypeScript compiles without errors
  </acceptance_criteria>
  <action>
    **SEC2-05 — Fix (req as any).user in notificationRoutes.ts (per D-12, D-13):**

    In `server/src/routes/notificationRoutes.ts`:
    - Add import: `import { validate } from '../middleware/validation.js';` after the existing imports.
    - Replace all 9 occurrences of `(req as any).user.userId` with `req.user!.userId`. These are at the start of every handler: lines 15, 42, 60, 84, 120, 145, 167, 203, 242. Every route is behind `router.use(authenticate())` at line 11, so `req.user` is always populated.
    - The `const userId = (req as any).user.userId;` pattern becomes `const userId = req.user!.userId;` at each location.

    **SEC2-04 — Add validation schemas to notificationRoutes.ts (per D-04, D-06):**

    Add validate() middleware to these endpoints:

    - `POST /subscribe`: validate body `{ endpoint: { type: 'url', required: true, maxLength: 500 }, keys: { type: 'object', required: true } }`
    - `DELETE /unsubscribe`: validate body `{ endpoint: { type: 'url', required: true, maxLength: 500 } }`
    - `POST /resubscribe`: validate body `{ endpoint: { type: 'url', required: true, maxLength: 500 }, keys: { type: 'object', required: true } }`
    - `GET /logs`: validate query `{ limit: { type: 'number', required: false, min: 1, max: 100 } }`
    - `PUT /preferences`: validate body `{ motion_enabled: { type: 'boolean', required: false }, face_enabled: { type: 'boolean', required: false }, object_enabled: { type: 'boolean', required: false }, quiet_hours_enabled: { type: 'boolean', required: false }, quiet_hours_start: { type: 'string', required: false, pattern: /^\d{2}:\d{2}$/ }, quiet_hours_end: { type: 'string', required: false, pattern: /^\d{2}:\d{2}$/ }, quiet_hours_timezone: { type: 'string', required: false, maxLength: 50 } }`
    - `GET /subscription`, `POST /test`, `GET /preferences`, `POST /preferences/reset`: these do not parse user input from body/query/params (beyond what authenticate provides), so no validation middleware needed.

    The middleware chain for each route becomes: `router.post('/subscribe', validate({...}), async (req, res) => {...})` — validate goes between the path and the handler. The `router.use(authenticate())` at line 11 already covers auth for all routes.

    **D-18/D-19 — Fix hardcoded timezone:**

    - Line 181: Replace `quietHoursTimezone: 'Asia/Kolkata'` with `quietHoursTimezone: process.env.TZ || 'UTC'`
    - Line 257: Replace `quietHoursTimezone: 'Asia/Kolkata'` with `quietHoursTimezone: process.env.TZ || 'UTC'`

    **SEC2-05 — Fix auditLogger.ts (req as any) patterns:**

    In `server/src/utils/auditLogger.ts`:
    - `getClientIP` method (line 114): Replace `(req as any).connection.remoteAddress` with `req.socket.remoteAddress`. Same for `(req as any).socket.remoteAddress` — `req.socket` is typed in Express 5. Replace `(req as any).connection.socket.remoteAddress` with just `req.socket.remoteAddress` (Express 5 consolidates connection into socket). The fallback chain becomes: `req.ip || req.socket?.remoteAddress || 'unknown'`.
    - `getUserId` method (line 122-124): Replace `(req as any).user?.sub || (req as any).user?.id` with `req.user?.userId`. The JWTPayload interface has `userId`, not `sub` or `id`.
    - `getUsername` method (line 127-129): Replace `(req as any).user?.username || (req as any).user?.name` with `req.user?.username`. The JWTPayload has `username`.
    - `getSessionId` method (line 132-133): Replace `(req as any).sessionID || (req as any).session?.id` with `undefined`. Express 5 sessions use a different mechanism — since session IDs are not part of the JWT auth flow, return `undefined`.
    - Line 166: `(req as any).headers['x-request-id']` — replace with `req.headers['x-request-id'] as string | undefined`. Express `req.headers` is typed.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -30 && echo "---" && grep -c '(req as any).user' src/routes/notificationRoutes.ts src/utils/auditLogger.ts</automated>
  </verify>
  <done>
    - Zero `(req as any).user` occurrences in notificationRoutes.ts and auditLogger.ts
    - All notification routes that parse body/query have validate() middleware
    - `Asia/Kolkata` replaced with `process.env.TZ || 'UTC'` in notificationRoutes.ts
    - auditLogger uses typed req.user, req.socket, req.headers access
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add validation to motion, detection-operations, highlights, visitor routes</name>
  <files>server/src/routes/motion.ts, server/src/routes/detection-operations.ts, server/src/routes/highlights.ts, server/src/routes/visitorRoutes.ts</files>
  <read_first>
    - server/src/routes/motion.ts (full file — 82 lines, 4 routes)
    - server/src/routes/detection-operations.ts (full file — 103 lines, 6 routes, already has inline validateCameraId)
    - server/src/routes/highlights.ts (full file — 61 lines, 2 routes, sort values injected into SQL)
    - server/src/routes/visitorRoutes.ts (full file — 71 lines, 5 routes)
    - server/src/middleware/validation.ts (lines 1-70 for validate() function, lines 172-361 for commonSchemas)
  </read_first>
  <acceptance_criteria>
    - `grep 'import.*validate.*from.*validation' server/src/routes/motion.ts` returns a match
    - `grep 'import.*validate.*from.*validation' server/src/routes/detection-operations.ts` returns a match
    - `grep 'import.*validate.*from.*validation' server/src/routes/highlights.ts` returns a match
    - `grep 'import.*validate.*from.*validation' server/src/routes/visitorRoutes.ts` returns a match
    - `grep 'validate(' server/src/routes/motion.ts` returns at least 3 matches (one per validated route)
    - `grep 'validate(' server/src/routes/highlights.ts` returns at least 2 matches
    - `grep 'validate(' server/src/routes/visitorRoutes.ts` returns at least 3 matches
    - highlights.ts sort param has enum constraint: `enum: ['recent', 'persons', 'faces', 'unknown', 'confidence']`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <action>
    **SEC2-04 — Add validate() middleware to route files (per D-04, D-05, D-06):**

    In each file, add `import { validate } from '../middleware/validation.js';` to the imports. Then add validate() middleware to each route that parses user input. Schema details per file:

    **motion.ts (4 routes):**
    - `GET /events`: validate query `{ limit: { type: 'number', required: false, min: 1, max: 1000 } }`
    - `GET /:cameraId/events`: validate params `{ cameraId: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/ } }` plus query `{ limit: { type: 'number', required: false, min: 1, max: 1000 } }`
    - `POST /:cameraId/simulate`: validate params `{ cameraId: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/ } }`
    - `POST /:cameraId/analyze`: validate params `{ cameraId: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/ } }` plus body `{ enablePersonDetection: { type: 'boolean', required: false }, enableFaceDetection: { type: 'boolean', required: false } }`
    - Also fix line 64: `(req as any).app.get('io')` → `(req.app as any).get('io')` (req.app is typed, .get('io') returns any — acceptable for custom app setting per RESEARCH.md note)

    **detection-operations.ts (6 routes):**
    - `POST /person/:cameraId/trigger`: validate params `{ cameraId: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/ } }` — this replaces the inline validateCameraId call at lines 17-18; keep the inline check as defense-in-depth or remove it (executor discretion)
    - `POST /face/:cameraId/trigger`: validate params `{ cameraId: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/ } }`
    - `GET /person/settings`: validate query `{ camera: { type: 'string', required: false, maxLength: 100 } }`
    - `PUT /person/settings`: validate body `{ minConfidence: { type: 'number', required: false, min: 0, max: 1 }, maxDetections: { type: 'number', required: false, min: 1, max: 100 }, targetClasses: { type: 'array', required: false } }`
    - `GET /face/settings`: no user input — no validation needed
    - `PUT /face/settings`: validate body `{ recognitionThreshold: { type: 'number', required: false, min: 0, max: 1 }, minFaceSize: { type: 'number', required: false, min: 16, max: 500 } }`
    - Also fix lines 27, 48: `(req as any).app.get('io')` → `(req.app as any).get('io')`

    **highlights.ts (2 routes — HIGH priority due to sort param in SQL):**
    - `GET /:date`: validate params `{ date: { type: 'string', required: true, pattern: /^\d{4}-\d{2}-\d{2}$/ } }` plus query `{ sort: { type: 'string', required: false, enum: ['recent', 'persons', 'faces', 'unknown', 'confidence'] }, limit: { type: 'number', required: false, min: 1, max: 1000 } }`
    - `GET /:date/summary`: validate params `{ date: { type: 'string', required: true, pattern: /^\d{4}-\d{2}-\d{2}$/ } }`

    **visitorRoutes.ts (5 routes):**
    - `GET /list`: no user input — no validation needed
    - `GET /timeline`: no user input — no validation needed
    - `GET /:id`: validate params `{ id: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/, maxLength: 100 } }`
    - `PUT /:id`: validate params `{ id: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/, maxLength: 100 } }` plus body `{ name: { type: 'string', required: true, minLength: 1, maxLength: 100 } }`
    - `DELETE /:id`: validate params `{ id: { type: 'string', required: true, pattern: /^[a-zA-Z0-9_-]+$/, maxLength: 100 } }`
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - motion.ts: 4 routes have validate() middleware, (req as any).app.get('io') fixed
    - detection-operations.ts: 4 routes with user input have validate() middleware, (req as any).app.get('io') fixed
    - highlights.ts: 2 routes have validate() with sort enum constraint preventing SQL injection
    - visitorRoutes.ts: 3 routes with user input have validate() middleware
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add validation to remaining routes + camera config warning + env consolidation</name>
  <files>server/src/routes/event-search.ts, server/src/routes/faceConfigRoutes.ts, server/src/routes/faceEmbeddingRoutes.ts, server/src/routes/detectionRedoRoutes.ts, server/src/routes/nvidiaRoutes.ts, server/src/config/index.ts, .env.example</files>
  <read_first>
    - server/src/routes/event-search.ts (full file — 161 lines, 9 routes)
    - server/src/routes/faceConfigRoutes.ts (full file — 159 lines, 4 routes)
    - server/src/routes/faceEmbeddingRoutes.ts (full file — 185 lines, 5 routes)
    - server/src/routes/detectionRedoRoutes.ts (full file — 311 lines, 2 routes with filepath input)
    - server/src/routes/nvidiaRoutes.ts (full file — 19 lines, delegates to controller)
    - server/src/config/index.ts (lines 274-313 for camera config IIFE)
    - .env.example (full file — 329 lines)
    - server/.env.example (full file — 80 lines)
    - server/src/middleware/validation.ts (lines 1-70 for validate() function)
  </read_first>
  <acceptance_criteria>
    - `grep 'import.*validate.*from.*validation' server/src/routes/event-search.ts` returns a match
    - `grep 'import.*validate.*from.*validation' server/src/routes/faceConfigRoutes.ts` returns a match
    - `grep 'import.*validate.*from.*validation' server/src/routes/faceEmbeddingRoutes.ts` returns a match
    - `grep 'validate(' server/src/routes/event-search.ts` returns at least 5 matches
    - `grep 'validate(' server/src/routes/faceConfigRoutes.ts` returns at least 1 match
    - `grep 'validate(' server/src/routes/faceEmbeddingRoutes.ts` returns at least 2 matches
    - `grep 'logger.warn' server/src/config/index.ts` returns a match in the cameras IIFE
    - `.env.example` contains `SEED_ADMIN_PASSWORD`, `SEED_USER_PASSWORD`, `OPENCV_SERVICE_URL`, `DETECTIONS_DIR`, `CREDENTIAL_ENCRYPTION_KEY`, `NVIDIA_API_KEY`, `PIPELINE_MODE`, `PYTHON_WS_URL`, `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`
    - `test -f server/.env.example` returns exit code 1 (file removed)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <action>
    **SEC2-04 — Validation for remaining route files (per D-04, D-05, D-06):**

    In each file, add `import { validate } from '../middleware/validation.js';`. Schema details:

    **event-search.ts (9 routes):**
    - `GET /search`: validate query `{ startDate: { type: 'string', required: false, pattern: /^\d{4}-\d{2}-\d{2}/ }, endDate: { type: 'string', required: false, pattern: /^\d{4}-\d{2}-\d{2}/ }, cameraId: { type: 'string', required: false, pattern: /^[a-zA-Z0-9_-]+$/ }, eventType: { type: 'string', required: false }, page: { type: 'number', required: false, min: 1 }, pageSize: { type: 'number', required: false, min: 1, max: 100 }, sortBy: { type: 'string', required: false }, sortOrder: { type: 'string', required: false, enum: ['ASC', 'DESC', 'asc', 'desc'] } }`
    - `GET /search/legacy`: validate query `{ page: { type: 'number', required: false, min: 1 }, pageSize: { type: 'number', required: false, min: 1, max: 100 }, cameraId: { type: 'string', required: false }, searchQuery: { type: 'string', required: false, maxLength: 200 }, startDate: { type: 'string', required: false }, endDate: { type: 'string', required: false } }`
    - `GET /stats/today`: no user input — no validation
    - `GET /stats/calendar`: validate query `{ year: { type: 'number', required: false, min: 2020, max: 2100 }, month: { type: 'number', required: false, min: 1, max: 12 }, camera_id: { type: 'string', required: false, pattern: /^[a-zA-Z0-9_-]+$/ } }`
    - `GET /stats/range`: validate query `{ start_date: { type: 'string', required: true, pattern: /^\d{4}-\d{2}-\d{2}/ }, end_date: { type: 'string', required: true, pattern: /^\d{4}-\d{2}-\d{2}/ }, camera_id: { type: 'string', required: false } }`
    - `GET /list`: no user input — no validation
    - `GET /:id/details`: validate params `{ id: { type: 'string', required: true, minLength: 1, maxLength: 100 } }`
    - `GET /image/:filename`: already has inline path traversal validation — add validate params `{ filename: { type: 'string', required: true, pattern: /^[a-zA-Z0-9._-]+$/ } }`
    - `POST /:id/archive`: validate params `{ id: { type: 'string', required: true, minLength: 1, maxLength: 100 } }`

    **faceConfigRoutes.ts (4 routes):**
    - `GET /`: no user input — no validation
    - `GET /:key`: validate params `{ key: { type: 'string', required: true, minLength: 1, maxLength: 100 } }`
    - `PUT /:key`: validate params `{ key: { type: 'string', required: true, minLength: 1, maxLength: 100 } }` plus body `{ value: { type: 'number', required: true } }`
    - `POST /reset`: no user input — no validation

    **faceEmbeddingRoutes.ts (5 routes):**
    - `POST /`: validate body `{ visitorId: { type: 'string', required: true }, embeddingVector: { type: 'array', required: true }, qualityScore: { type: 'number', required: false }, cameraId: { type: 'string', required: false }, embeddingVersion: { type: 'string', required: false, enum: ['128', '512'] } }` (the route already validates array length internally — keep that)
    - `GET /visitor/:visitorId`: validate params `{ visitorId: { type: 'string', required: true, minLength: 1 } }` plus query `{ minQuality: { type: 'number', required: false, min: 0, max: 100 }, version: { type: 'string', required: false, enum: ['128', '512'] } }`
    - `GET /high-quality`: validate query `{ visitorId: { type: 'string', required: false }, minQuality: { type: 'number', required: false, min: 0, max: 100 }, limit: { type: 'number', required: false, min: 1, max: 50 } }`
    - `DELETE /:id`: validate params `{ id: { type: 'string', required: true, minLength: 1 } }`
    - `GET /stats`: no user input — no validation

    **detectionRedoRoutes.ts (2 routes):**
    - `POST /rerun-detection`: validate body `{ filename: { type: 'string', required: false, maxLength: 255 }, filepath: { type: 'string', required: false, maxLength: 500 } }` with custom validator that rejects paths containing `..` or starting with `/`: `custom: (v) => !v.includes('..') || 'Path traversal not allowed'`
    - `POST /rerun-event-detection`: validate body `{ eventId: { type: 'string', required: true, minLength: 1, maxLength: 100 } }`

    **nvidiaRoutes.ts (delegating routes — minimal validation):**
    - This file delegates entirely to nvidiaController. Add `import { validate } from '../middleware/validation.js';` and basic validation for the body-heavy POST endpoints:
    - `POST /analyze`, `POST /analyze-event`, `POST /analyze-with-bboxes`, `POST /analyze-persons`, `POST /analyze-event-with-bboxes`: validate body `{ image: { type: 'string', required: false }, imagePath: { type: 'string', required: false } }` — at minimum ensure body is an object. These are authenticated internal routes, so minimal validation is acceptable.
    - `PUT /config`: validate body as object type (the controller handles specifics).

    **SEC2-06 — Camera config warning (per D-14, D-15):**

    In `server/src/config/index.ts`, inside the cameras IIFE (lines 274-313):
    - Import or use the existing `logger` from `../utils/logger.js` — check if logger is already imported at the top of the file. If not, add `import { logger } from '../utils/logger.js';`.
    - After line 306 (the `cameras = cameras.map(...)` call) and before line 308 (`return cameras`): add a check `if (cameras.length === 0) { logger.warn('Camera configuration loaded but resulted in 0 cameras. Check cameras.json format or CAMERAS env var. Server will start but no cameras will be active.', 'Config'); }`
    - In the catch block (line 309-312): after `console.error(...)`, add `logger.warn('Camera configuration failed to load. Server starting with no cameras. Verify cameras.json exists and contains valid JSON.', 'Config');`
    - Do NOT throw or exit — only log warnings per D-14.

    **D-16/D-17 — Env file consolidation:**

    Update `.env.example` at project root:
    - Add these missing env vars (present in server/.env.example or used by code but missing from root):
      - `CREDENTIAL_ENCRYPTION_KEY=` (with comment: `# AES-256-GCM key for encrypting stored RTSP credentials`)
      - `NVIDIA_API_KEY=` with comment `# NVIDIA Build API key for AI vision analysis`
      - `NVIDIA_MODEL=nvidia/nemotron-3-nano-omni-30b-a3b-reasoning`
      - `FRAME_INTERVAL=40` with comment `# Streaming frame interval in ms`
      - `MAX_RECONNECT_ATTEMPTS=5`
      - `RECONNECT_DELAY=5000`
      - `OPENCV_SERVICE_URL=http://localhost:8084` with comment `# OpenCV Python service URL`
      - `PIPELINE_MODE=python-only` with comment `# Detection pipeline: legacy, dual, python-only`
      - `PYTHON_WS_URL=ws://localhost:9090` with comment `# Python WebSocket URL for frame relay`
      - `DETECTIONS_DIR=./data/detections` with comment `# Directory for detection image storage`
      - `DETECTIONS_RETENTION_DAYS=30` with comment `# Days to retain detection files`
      - `VAPID_PUBLIC_KEY=` with comment `# Web Push VAPID keys for browser notifications`
      - `VAPID_PRIVATE_KEY=`
      - `VAPID_SUBJECT=mailto:admin@your-domain.com`
      - `SEED_ADMIN_PASSWORD=` with comment `# REQUIRED in production — server will not start without these` (replace the existing weak `SEED_ADMIN_PASSWORD=admin123`)
      - `SEED_USER_PASSWORD=` with comment `# REQUIRED in production`
    - Fix the existing `SEED_ADMIN_PASSWORD=admin123` line to remove the weak default value.
    - Fix `DB_NAME=sentryvision` (root already has this — verify).
    - Fix `JWT_ACCESS_SECRET` and `JWT_REFRESH_SECRET` (root already has these — verify).
    - Remove `server/.env.example` file after consolidation.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -30 && echo "---" && test -f .env.example && echo "root .env.example exists" && test -f server/.env.example && echo "server/.env.example still exists (BUG)" || echo "server/.env.example removed (OK)"</automated>
  </verify>
  <done>
    - event-search.ts: routes with user input have validate() middleware
    - faceConfigRoutes.ts: PUT /:key has validate() for params and body
    - faceEmbeddingRoutes.ts: POST, GET /visitor/:visitorId, GET /high-quality, DELETE /:id have validate()
    - detectionRedoRoutes.ts: both POST routes have validate() with path traversal protection
    - nvidiaRoutes.ts: POST routes have basic body validation
    - config/index.ts: logger.warn called when cameras array is empty (both success and error paths)
    - .env.example: consolidated with all env vars from server/.env.example plus code-discovered vars
    - server/.env.example: removed
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Route handlers | Untrusted req.body, req.query, req.params input |
| highlights.ts sort param → SQL | User-supplied string interpolated into ORDER BY clause |
| detectionRedoRoutes.ts filepath → filesystem | User-supplied path for file read |
| Server startup → cameras.json | Config file read at module evaluation time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | highlights.ts sort param | mitigate | validate() with enum constraint ['recent','persons','faces','unknown','confidence'] — prevents arbitrary SQL in ORDER BY |
| T-07-05 | Tampering | detectionRedoRoutes.ts filepath | mitigate | validate() body with custom validator rejecting '..' paths + existing fs.access check |
| T-07-06 | Tampering, Spoofing | All route inputs | mitigate | validate() middleware on every route parsing body/query/params — consistent schema-based validation (SEC2-04, D-04) |
| T-07-07 | Information Disclosure | Camera config silent failure | mitigate | logger.warn on empty cameras array — operators can detect config issues (SEC2-06, D-14) |
| T-07-08 | Tampering | (req as any).user bypass | mitigate | Replace with req.user!.userId using Express Request augmentation — compile-time type safety (SEC2-05, D-12) |
| T-07-SC | Tampering | npm install (no new packages) | accept | No new packages — all validation uses existing middleware/validation.ts |
</threat_model>

<verification>
1. TypeScript compiles: `cd server && npx tsc --noEmit`
2. No (req as any).user in notificationRoutes: `grep -c '(req as any).user' server/src/routes/notificationRoutes.ts` → 0
3. No (req as any).user in auditLogger: `grep -c '(req as any).user' server/src/utils/auditLogger.ts` → 0
4. No Asia/Kolkata hardcoded: `grep 'Asia/Kolkata' server/src/routes/notificationRoutes.ts` → empty
5. Camera config warning: `grep 'logger.warn' server/src/config/index.ts` → match in cameras IIFE
6. Validation on all route files: `grep -l 'validate(' server/src/routes/*.ts | wc -l` → 12+ files (auth, cameras, notification, motion, detection-operations, highlights, visitors, event-search, faceConfig, faceEmbedding, detectionRedo, nvidia)
7. Env consolidated: `test -f server/.env.example` → exit 1
8. Seed password in env: `grep 'SEED_ADMIN_PASSWORD' .env.example` → match
9. Existing tests pass: `cd server && npm run test:server`
</verification>

<success_criteria>
- Every route file that parses req.body/query/params has validate() middleware with explicit schemas
- Zero (req as any).user occurrences in notificationRoutes.ts and auditLogger.ts
- highlights.ts sort param constrained to enum values (preventing SQL injection)
- detectionRedoRoutes.ts filepath validated against path traversal
- Empty cameras.json produces logger.warn instead of silent empty array
- Timezone defaults to process.env.TZ instead of hardcoded 'Asia/Kolkata'
- Single consolidated .env.example at project root with all required env vars
- server/.env.example removed
- TypeScript compiles without errors
- Existing tests pass
</success_criteria>

<output>
Create `.planning/phases/07-phase-7-security-hardening/07-02-SUMMARY.md` when done
</output>
