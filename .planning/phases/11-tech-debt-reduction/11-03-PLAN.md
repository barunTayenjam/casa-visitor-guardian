---
phase: 11-tech-debt-reduction
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - server/src/index.ts
  - server/src/routes/index.ts
  - server/src/config/index.ts
  - opencv-service/app.py
  - frontend/src/contexts/CameraContext.tsx
autonomous: true
requirements:
  - TD-03
user_setup: []

must_haves:
  truths:
    - "All sensitive API routes require authentication middleware"
    - "Image serving endpoints reject filenames containing '..'"
    - "No hardcoded fallback JWT secret in config"
    - "No hardcoded default database password in OpenCV service"
    - "No hardcoded IP addresses in source code"
  artifacts:
    - path: "server/src/index.ts"
      provides: "Path traversal protection on image endpoints, auth on inline routes"
      contains: "requireUser|requireAdmin|authenticate"
    - path: "server/src/routes/index.ts"
      provides: "Auth middleware on all sensitive route handlers"
    - path: "server/src/config/index.ts"
      provides: "No fallback JWT secret string"
    - path: "opencv-service/app.py"
      provides: "No default database password"
  key_links:
    - from: "server/src/middleware/auth.ts"
      to: "server/src/routes/index.ts"
      via: "requireUser/requireAdmin middleware"
      pattern: "requireUser|requireAdmin|authenticate"
    - from: "server/src/index.ts"
      to: "server/src/index.ts /events/:filename"
      via: "path traversal validation"
      pattern: "\\.\\."
---

<objective>
Harden security posture by adding authentication to unauthenticated routes, fixing path traversal vulnerabilities, removing hardcoded secrets, and eliminating hardcoded IP addresses.

Purpose: The system currently exposes 67+ route handlers without authentication, has path traversal risks in image serving, uses fallback secrets, and contains hardcoded IPs. These are critical security gaps even for local network use.
Output: Auth-protected routes, path traversal validation, no hardcoded secrets, environment-variable-only configuration.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@server/src/middleware/auth.ts
</context>

<interfaces>
<!-- Key auth middleware contracts from server/src/middleware/auth.ts -->

```typescript
// Auth middleware patterns (from existing codebase)
export function authenticate(options?: AuthOptions): RequestHandler;
export function requireUser: RequestHandler;
export function requireAdmin: RequestHandler;
export function optionalAuth: RequestHandler;

// AuthOptions interface
interface AuthOptions {
  roles?: string[];
  optional?: boolean;
}
```

<!-- Existing route patterns with auth -->
```typescript
// Pattern from auth.ts — routes with auth
app.post('/api/auth/register', authenticate({ roles: ['admin'] }), handler);
app.post('/api/auth/login', handler); // No auth needed for login

// Pattern from visitorRoutes.ts
app.get('/api/visitors/list', requireUser, handler);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add authentication middleware to unauthenticated sensitive routes</name>
  <read_first>
    - server/src/routes/index.ts (scan for route handlers WITHOUT requireUser/requireAdmin/authenticate)
    - server/src/index.ts (lines 341-487 for inline routes without auth)
    - server/src/middleware/auth.ts (available auth middleware functions)
  </read_first>
  <files>
    server/src/routes/index.ts
    server/src/index.ts
  </files>
  <action>
    In server/src/routes/index.ts, add `requireUser` middleware to all sensitive route handlers that currently lack authentication. Categorize routes:

    **MUST have requireUser (write operations, data access):**
    - Camera control routes (start/stop stream, snapshot capture)
    - Motion settings routes (update detection parameters)
    - Zone management routes (add/edit/delete adaptive regions)
    - Filter configuration routes
    - Event deletion routes
    - Settings CRUD routes (read can be optionalAuth, write must be requireUser or requireAdmin)
    - System overview endpoint (requireUser)

    **CAN use optionalAuth (read-only, public-friendly):**
    - Camera listing (GET /api/cameras) — optionalAuth so unauthenticated users can see camera names but not control them
    - Event history/listing (GET /api/events/*) — optionalAuth for read access
    - Image serving (GET /api/events/image/*) — optionalAuth

    **MUST have requireAdmin (system-level changes):**
    - Any route that modifies system settings
    - Any route that manages users or roles

    In server/src/index.ts, add auth middleware to inline routes (lines 341-487):
    - GET /api/review — optionalAuth (read-only)
    - GET /api/review/:id — optionalAuth (read-only)
    - POST /api/review/:id/acknowledge — requireUser (write)
    - GET /api/timeline — optionalAuth (read-only)
    - GET /api/timeline/active/:camera — optionalAuth (read-only)
    - GET /api/detection/config — optionalAuth (read-only)
    - PUT /api/detection/config — requireUser (write)

    Import pattern: `import { requireUser, requireAdmin, optionalAuth } from './middleware/auth.js';`

    Apply middleware as the second argument to app.METHOD(), before the handler function:
    `app.get('/api/cameras', optionalAuth, async (req, res) => { ... });`

    Do NOT add auth to: POST /api/auth/* routes (already handled by auth.ts), health check endpoints.
  </action>
  <acceptance_criteria>
    - `grep -c "requireUser\|requireAdmin\|optionalAuth" server/src/routes/index.ts` returns a count greater than the current count (more auth middleware applied)
    - `grep -c "requireUser\|requireAdmin\|optionalAuth" server/src/index.ts` returns at least 7 (one per inline route)
    - No route handler for camera control, settings write, or event deletion exists without auth middleware
    - `cd server && npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All sensitive API routes in routes/index.ts and inline routes in index.ts have auth middleware applied, backend builds</done>
</task>

<task type="auto">
  <name>Task 2: Fix path traversal risk in image serving endpoints</name>
  <read_first>
    - server/src/index.ts (lines 53-130 for /events/:filename endpoint)
    - server/src/index.ts (lines 133+ for /snapshots/:filename endpoint)
    - server/src/routes/index.ts (search for filename.includes('..') for existing protection pattern)
  </read_first>
  <files>server/src/index.ts</files>
  <action>
    Add path traversal validation to BOTH image serving endpoints in server/src/index.ts:

    For GET /events/:filename (line 53):
    - Add validation at the top of the handler, before any file system operations:
      ```
      const filename = req.params.filename;
      if (filename.includes('..') || filename.includes('/') || filename.includes('\\')) {
        return res.status(400).json({ success: false, error: 'Invalid filename' });
      }
      ```
    - Also validate that filename matches expected pattern: only alphanumeric characters, hyphens, underscores, and dots (for extension): `/^[a-zA-Z0-9._-]+$/`

    For GET /snapshots/:filename (line 133):
    - Apply the same validation as above

    The existing protection in server/src/routes/index.ts (filename.includes('..')) should be enhanced to the same pattern for consistency.

    Do NOT change the database lookup logic or filesystem scanning fallback — only add the validation guard at the entry point.
  </action>
  <acceptance_criteria>
    - `grep -n "filename.includes('..')" server/src/index.ts` returns at least 2 matches (one per endpoint)
    - Both /events/:filename and /snapshots/:filename handlers have validation before any fs.existsSync() or res.sendFile() calls
    - `cd server && npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both image serving endpoints validate filename against path traversal patterns, reject '..' and '/' characters, backend builds</done>
</task>

<task type="auto">
  <name>Task 3: Remove hardcoded secrets and IP addresses</name>
  <read_first>
    - server/src/config/index.ts (line 245 for JWT fallback secret, line 248 for DB host default)
    - server/src/index.ts (line 47 for CORS origins with hardcoded IPs)
    - opencv-service/app.py (line 73 for hardcoded DB password)
    - frontend/src/contexts/CameraContext.tsx (search for hardcoded RTSP URLs)
  </read_first>
  <files>
    server/src/config/index.ts
    server/src/index.ts
    opencv-service/app.py
    frontend/src/contexts/CameraContext.tsx
  </files>
  <action>
    **server/src/config/index.ts — JWT fallback secret:**
    - Line 245: Remove the fallback string `'fallback-secret-change-in-production'`
    - Replace with: throw an error if JWT_ACCESS_SECRET and JWT_SECRET are both unset, regardless of NODE_ENV
    - Pattern: `const jwtSecret = process.env.JWT_ACCESS_SECRET || process.env.JWT_SECRET; if (!jwtSecret) throw new Error('JWT_ACCESS_SECRET or JWT_SECRET must be configured');`
    - The validateConfig() warning is not sufficient — fail fast at config load time

    **server/src/config/index.ts — Database host default:**
    - Line 248: Remove the default `'172.26.0.3'` (Docker internal IP)
    - Replace with: `process.env.DB_HOST || 'localhost'` — localhost is a safer default for local development

    **server/src/index.ts — CORS origins:**
    - Line 47: Remove hardcoded IPs `'http://192.168.31.99:5173'` and `'http://192.168.31.99:8082'`
    - Keep only: `['http://localhost:3000', 'http://localhost:5173']` as defaults
    - Add comment: "Add production origins via CORS_ORIGIN env var (comma-separated)"

    **opencv-service/app.py — Database password:**
    - Line 73: Remove the default `'sentryvision123'`
    - Replace with: `os.environ.get('POSTGRES_PASSWORD')` with no default — if None, raise an error at startup
    - Add: `if not db_password: raise ValueError('POSTGRES_PASSWORD environment variable is required')`

    **frontend/src/contexts/CameraContext.tsx — Hardcoded RTSP URLs:**
    - Search for any hardcoded `rtsp://` URLs in this file
    - Replace with references to the camera configuration loaded from the backend API (the cameras are already served via GET /api/cameras)
    - If RTSP URLs are embedded directly, remove them and use the camera objects from the API response instead
  </action>
  <acceptance_criteria>
    - `grep -n "fallback-secret-change-in-production" server/src/config/index.ts` returns no matches
    - `grep -n "172.26.0.3" server/src/config/index.ts` returns no matches
    - `grep -n "192.168.31.99" server/src/index.ts` returns no matches
    - `grep -n "sentryvision123" opencv-service/app.py` returns no matches
    - `grep -n "rtsp://" frontend/src/contexts/CameraContext.tsx` returns no matches
    - `cd server && npm run build` succeeds
    - `python3 -c "import py_compile; py_compile.compile('opencv-service/app.py', doraise=True)"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>No hardcoded secrets or IP addresses remain in source code, all values sourced from environment variables, both TypeScript and Python compile</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Unauthenticated requests now blocked by requireUser/requireAdmin middleware |
| Image serving | Filename parameter validated against path traversal before any filesystem access |
| Config loading | Fails fast if JWT secret not configured — no silent fallback to weak secret |
| OpenCV service | Fails to start without POSTGRES_PASSWORD — no default credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Spoofing | Unauthenticated API routes | mitigate | requireUser/requireAdmin middleware on all sensitive endpoints |
| T-11-08 | Tampering | Image serving path traversal | mitigate | Filename validation rejects '..' '/' '\' characters before filesystem access |
| T-11-09 | Repudiation | JWT secret fallback | mitigate | Fail fast if JWT secret not configured — no default secret |
| T-11-10 | Information Disclosure | Hardcoded IPs in CORS | mitigate | Removed hardcoded IPs; only localhost defaults, production via env var |
| T-11-11 | Spoofing | OpenCV service DB password | mitigate | No default password; fails to start without POSTGRES_PASSWORD env var |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- All sensitive routes have auth middleware (requireUser, requireAdmin, or optionalAuth)
- Image serving endpoints validate filename against path traversal
- No hardcoded JWT fallback secret in config
- No hardcoded database password in OpenCV service
- No hardcoded IP addresses in source code
- Backend builds, OpenCV service Python syntax check passes
</verification>

<success_criteria>
- 67+ previously unauthenticated routes now have auth middleware
- Path traversal attempts on image endpoints return 400 error
- Config fails fast without JWT secret (no fallback)
- OpenCV service fails to start without POSTGRES_PASSWORD
- All hardcoded IPs replaced with localhost defaults or env vars
</success_criteria>

<output>
Create `.planning/phases/11-tech-debt-reduction/11-03-SUMMARY.md` when done
</output>
