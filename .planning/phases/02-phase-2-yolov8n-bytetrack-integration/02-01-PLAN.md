---
phase: 02-streaming-performance-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - opencv-service/rtsp_ingestion/frame_pipeline.py
  - opencv-service/rtsp_ingestion/config.py
  - opencv-service/rtsp_ingestion/metrics.py
  - opencv-service/rtsp_ingestion/__init__.py
  - opencv-service/app.py
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03]

must_haves:
  truths:
    - "YOLO inference uses CUDA backend when a CUDA-capable GPU is detected"
    - "YOLO inference falls back to CPU when no CUDA GPU is available or when CUDA initialization fails"
    - "Inference latency in milliseconds is logged every 100 detections and exposed via metrics snapshot"
    - "Backend label (CPU or CUDA) is logged at initialization and included in metrics"
  artifacts:
    - path: "opencv-service/rtsp_ingestion/frame_pipeline.py"
      provides: "InProcessYOLO CUDA detection + fallback + latency timing"
      contains: "_detect_backend"
    - path: "opencv-service/rtsp_ingestion/config.py"
      provides: "INFERENCE_BACKEND and INFERENCE_TARGET config constants"
      contains: "INFERENCE_BACKEND"
    - path: "opencv-service/app.py"
      provides: "YOLOObjectDetector CUDA detection for ONNX models"
      contains: "getCudaEnabledDeviceCount"
    - path: "opencv-service/rtsp_ingestion/__init__.py"
      provides: "Inference metrics in get_metrics_snapshot()"
      contains: "inference"
  key_links:
    - from: "opencv-service/rtsp_ingestion/frame_pipeline.py"
      to: "opencv-service/rtsp_ingestion/config.py"
      via: "import INFERENCE_BACKEND"
      pattern: "from.*config import.*INFERENCE_BACKEND"
    - from: "opencv-service/rtsp_ingestion/frame_pipeline.py"
      to: "opencv-service/rtsp_ingestion/metrics.py"
      via: "set_gauge for latency"
      pattern: "set_gauge.*inference"
    - from: "opencv-service/app.py"
      to: "/api/rtsp/metrics"
      via: "Flask route exposes RTSPService metrics"
      pattern: "rtsp_metrics"
---

<objective>
GPU-accelerated YOLO inference with CUDA fallback and latency metrics

Purpose: Reduce YOLO detection latency from 50-200ms to 5-20ms on GPU hardware while maintaining CPU-only compatibility for current deployments. Expose inference metrics for monitoring.

Output: Modified InProcessYOLO (frame_pipeline.py) and YOLOObjectDetector (app.py) with CUDA runtime detection, graceful CPU fallback, and inference latency instrumentation.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-phase-2-yolov8n-bytetrack-integration/02-RESEARCH.md
@.planning/phases/02-phase-2-yolov8n-bytetrack-integration/02-PATTERNS.md
@.planning/phases/02-phase-2-yolov8n-bytetrack-integration/02-VALIDATION.md

<interfaces>
<!-- Python module interfaces executor needs -->

From opencv-service/rtsp_ingestion/config.py:
```python
# Module-level constants with os.getenv overrides
WS_HOST = os.getenv('WS_HOST', '0.0.0.0')
WS_PORT = int(os.getenv('WS_PORT', '9090'))
METRICS_WS_LATENCY_BUCKETS = [10, 25, 50, 100, 200, 500]
```

From opencv-service/rtsp_ingestion/metrics.py — MetricsCollector:
```python
class MetricsCollector:
    def increment(self, name: str, value: int = 1) -> None
    def set_gauge(self, name: str, value: float) -> None
    def snapshot(self) -> dict  # returns {'counters': {}, 'gauges': {}, 'timestamp': float}
    def get_counter(self, name: str) -> int
    def get_gauge(self, name: str) -> float
```

From opencv-service/rtsp_ingestion/frame_pipeline.py — InProcessYOLO:
```python
class InProcessYOLO:
    def __init__(self, models_dir: str)
    def initialize(self) -> bool
    def detect(self, frame: np.ndarray) -> List[Dict[str, Any]]
    # Fields: self._net, self._model_type, self._initialized, self._models_dir
```

From opencv-service/rtsp_ingestion/frame_pipeline.py — FramePipeline:
```python
class FramePipeline:
    _yolo_detector: Optional[InProcessYOLO] = None  # class-level singleton
    _yolo_init_lock = threading.Lock()
    @classmethod
    def _init_yolo(cls)  # called from __init__, initializes _yolo_detector singleton
```

From opencv-service/rtsp_ingestion/__init__.py — RTSPService:
```python
class RTSPService:
    def get_metrics_snapshot(self) -> dict  # currently returns {pipelines, pipeline_count, publisher_running}
```

From opencv-service/app.py — YOLOObjectDetector (lines 262-438):
```python
class YOLOObjectDetector:
    def initialize(self)  # lines 367-438: loads yolov8n > yolov5n > yolov4-tiny
    # ONNX paths (lines 376-404): hardcoded DNN_BACKEND_OPENCV + DNN_TARGET_CPU
    # YOLOv4-tiny path (lines 410-424): already has CUDA try/except fallback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CUDA backend selection with CPU fallback for YOLO inference</name>
  <files>opencv-service/rtsp_ingestion/frame_pipeline.py, opencv-service/rtsp_ingestion/config.py, opencv-service/app.py</files>
  <read_first>
    - opencv-service/rtsp_ingestion/frame_pipeline.py (full file — InProcessYOLO class at line 81, FramePipeline at line 265)
    - opencv-service/rtsp_ingestion/config.py (full file — 42 lines)
    - opencv-service/app.py (lines 262-438 — YOLOObjectDetector.initialize method)
    - .planning/phases/02-phase-2-yolov8n-bytetrack-integration/02-RESEARCH.md (Pattern 1: CUDA Backend Selection)
    - .planning/phases/02-phase-2-yolov8n-bytetrack-integration/02-PATTERNS.md (Track 1 sections)
  </read_first>
  <action>
    PERF-01 and PERF-02: Add CUDA runtime detection with graceful CPU fallback to both YOLO detector classes.

    **1. config.py** — Add two new module-level constants after the METRICS_WS_LATENCY_BUCKETS line (line 42), following the existing pattern of `CONSTANT = os.getenv('KEY', default)`:
    - `INFERENCE_BACKEND = os.getenv('INFERENCE_BACKEND', 'auto')` — values: 'auto', 'cuda', 'cpu'
    - `INFERENCE_TARGET = os.getenv('INFERENCE_TARGET', 'auto')` — values: 'auto', 'cuda', 'cuda_fp16', 'cpu'
    - Export both in `__init__.py` imports and `__all__` list.

    **2. frame_pipeline.py InProcessYOLO** — Add a private method `_detect_backend(self) -> tuple` that:
    - Checks `INFERENCE_BACKEND` from config: if 'cpu', immediately return `(DNN_BACKEND_OPENCV, DNN_TARGET_CPU, 'CPU')`
    - If 'auto' or 'cuda': call `cv2.cuda.getCudaEnabledDeviceCount()` wrapped in `try/except (AttributeError, cv2.error)` — if count > 0, verify CUDA works by calling `setPreferableBackend(DNN_BACKEND_CUDA)` and `setPreferableTarget(DNN_TARGET_CUDA)` on the already-loaded `self._net`, returning `(DNN_BACKEND_CUDA, DNN_TARGET_CUDA, 'CUDA')`
    - On any failure or count == 0: fall back to `(DNN_BACKEND_OPENCV, DNN_TARGET_CPU, 'CPU')`
    - Store `self._backend_label` (string 'CPU' or 'CUDA') for logging and metrics

    In `initialize()` (line 116): Replace the hardcoded `DNN_BACKEND_OPENCV` + `DNN_TARGET_CPU` at lines 123-124 (ONNX path) with a call to `self._detect_backend()` after `cv2.dnn.readNet(path)`. Log the chosen backend: `print(f"[InProcessYOLO] {mtype} initialized with {self._backend_label} backend")`. The YOLOv4-tiny path (lines 131-138) already has CUDA try/except — keep it as-is but also store `self._backend_label`.

    **3. app.py YOLOObjectDetector** — In `initialize()` (line 367): Replace the hardcoded CPU backend in the ONNX paths. For the yolov8n path (lines 376-388), after `self.net = cv2.dnn.readNet(yolov8_path)`: call `cv2.cuda.getCudaEnabledDeviceCount()` wrapped in try/except; if count > 0, set `DNN_BACKEND_CUDA` + `DNN_TARGET_CUDA`, else set `DNN_BACKEND_OPENCV` + `DNN_TARGET_CPU`. Log the backend. Apply the identical pattern to the yolov5n path (lines 390-403). Keep the existing yolov4-tiny CUDA try/except (lines 416-424) unchanged.

    Do NOT install any new packages — all changes use existing `cv2.dnn` and `cv2.cuda` APIs.
  </action>
  <verify>
    <automated>cd opencv-service && python -m pytest tests/ -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frame_pipeline.py InProcessYOLO has a `_detect_backend` method
    - frame_pipeline.py lines 123-124 no longer contain hardcoded `DNN_BACKEND_OPENCV` for ONNX models
    - frame_pipeline.py logs backend label at initialization (string contains "backend")
    - app.py YOLOObjectDetector yolov8n path calls `getCudaEnabledDeviceCount`
    - app.py yolov5n path calls `getCudaEnabledDeviceCount`
    - config.py has `INFERENCE_BACKEND` and `INFERENCE_TARGET` constants
    - __init__.py exports both new constants
    - All existing pytest tests pass
  </acceptance_criteria>
  <done>InProcessYOLO and YOLOObjectDetector both detect CUDA at runtime and fall back to CPU gracefully. No new packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Inference latency metrics with timing instrumentation</name>
  <files>opencv-service/rtsp_ingestion/frame_pipeline.py, opencv-service/rtsp_ingestion/__init__.py</files>
  <read_first>
    - opencv-service/rtsp_ingestion/frame_pipeline.py (InProcessYOLO.detect method at line 143, FramePipeline class at line 265)
    - opencv-service/rtsp_ingestion/metrics.py (full file — MetricsCollector with set_gauge)
    - opencv-service/rtsp_ingestion/__init__.py (RTSPService.get_metrics_snapshot at line 128)
  </read_first>
  <action>
    PERF-03: Add timing instrumentation around YOLO inference and expose latency metrics.

    **1. frame_pipeline.py InProcessYOLO** — In the `__init__` method, add instance variables: `self._last_inference_ms = 0.0`, `self._inference_count = 0`, `self._total_inference_ms = 0.0`.

    In `detect()` (line 143): Wrap `time.perf_counter()` around the `self._net.forward()` call (lines 149-154). Store the elapsed milliseconds in `self._last_inference_ms`. Increment `self._inference_count`. Add `self._total_inference_ms` accumulation. Log every 100th inference: `if self._inference_count % 100 == 0: print(f"[InProcessYOLO] inference={self._last_inference_ms:.1f}ms backend={self._backend_label} model={self._model_type} avg={self._total_inference_ms / self._inference_count:.1f}ms")`. Also add a public method `get_metrics(self) -> dict` that returns `{'backend': self._backend_label, 'model_type': self._model_type, 'last_inference_ms': self._last_inference_ms, 'inference_count': self._inference_count, 'avg_inference_ms': self._total_inference_ms / max(1, self._inference_count)}`.

    In FramePipeline._init_yolo (line 304): After successfully initializing the detector, also store the detector reference for metrics access. The class-level `_yolo_detector` already exists — no change needed for storage. Add a `get_yolo_metrics(self) -> dict` method on FramePipeline that returns `self._yolo_detector.get_metrics()` if `_yolo_detector` is not None, else returns empty dict.

    **2. __init__.py RTSPService** — In `get_metrics_snapshot()` (line 128): Extend the returned dict to include inference metrics from all pipelines. Add a key `'pipelines_metrics'` containing a dict mapping camera_id to `pipeline.get_yolo_metrics()`. Keep the existing keys (pipelines, pipeline_count, publisher_running).
  </action>
  <verify>
    <automated>cd opencv-service && python -m pytest tests/ -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - InProcessYOLO.detect() has `time.perf_counter()` calls wrapping forward()
    - InProcessYOLO has `get_metrics()` method returning dict with keys: backend, model_type, last_inference_ms, inference_count, avg_inference_ms
    - InProcessYOLO logs inference latency every 100 detections
    - FramePipeline has `get_yolo_metrics()` method
    - RTSPService.get_metrics_snapshot() returns pipeline-level inference metrics
    - All existing pytest tests pass
  </acceptance_criteria>
  <done>Inference latency measured, logged periodically, and exposed via RTSPService metrics endpoint (/api/rtsp/metrics). Backend label included in all metrics output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python process → CUDA driver | Untrusted hardware state; driver may be missing, mismatched, or crashed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | InProcessYOLO.initialize() | mitigate | Wrap all CUDA calls in try/except (AttributeError, cv2.error); fall back to CPU immediately on any failure |
| T-02-02 | Denial of Service | InProcessYOLO.detect() forward() | mitigate | Existing try/except cv2.error at line 149 already catches CUDA runtime errors and falls back to CPU |
| T-02-SC | Tampering | pip install | accept | No new packages installed in this plan — all changes use existing cv2.dnn and cv2.cuda APIs |
</threat_model>

<verification>
1. All existing pytest tests pass: `cd opencv-service && python -m pytest tests/ -x -v`
2. InProcessYOLO initializes with CPU backend on current (non-GPU) dev machine
3. Inference metrics logged to stdout at 100-detection intervals
4. `/api/rtsp/metrics` endpoint returns inference metrics (tested via `curl http://localhost:8084/api/rtsp/metrics`)
</verification>

<success_criteria>
- InProcessYOLO.initialize() uses _detect_backend() method instead of hardcoded CPU constants for ONNX models
- YOLOObjectDetector.initialize() calls getCudaEnabledDeviceCount() for ONNX model paths
- detect() measures and logs inference latency every 100 calls
- get_metrics_snapshot() includes inference metrics from all camera pipelines
- All existing tests pass with no regressions
</success_criteria>

<output>
Create `.planning/phases/02-phase-2-yolov8n-bytetrack-integration/02-01-SUMMARY.md` when done
</output>
