# Plan 10.3: Medium-Priority Page Migrations — Summary

**Completed:** 2026-03-27
**Status:** Complete (3/3 tasks)

---

## Tasks Completed

### 10.3.1: Migrate VisitorTimeline.new.tsx
**Commit:** `feat(10-3): migrate VisitorTimeline to PageHeader, StatCard, PageLoading, EmptyState`

**Changes:**
- Replaced `ArrowLeft` icon-only back button with `PageHeader` (ChevronLeft + "Back" text)
- Replaced inline `<h1>` title with `PageHeader` component (with `Users` icon, subtitle, export action)
- Replaced 4 inline stat cards with `StatCard` components (Total Visitors, Known, Unknown, Days)
- Replaced hardcoded spinner with `PageLoading` component
- Replaced inline empty state with `EmptyState` component
- Replaced error state with theme-aware Card layout
- Removed `colors` design-tokens import — all inline `style={{}}` color references replaced with Tailwind classes
- Replaced `colors.background.primary` → `bg-background`
- Replaced `colors.background.secondary` → `bg-card` (Card handles this)
- Replaced `colors.background.tertiary` → `bg-muted`
- Replaced `colors.text.primary` → `text-foreground`
- Replaced `colors.text.secondary`/`colors.text.muted` → `text-muted-foreground`
- Replaced `colors.status.*` → `text-blue-500`, `text-emerald-500`, `text-red-500`, `text-amber-500`
- Replaced `colors.border.subtle` → `border-border`
- Used `cn()` for dynamic visitor type badge classes
- Removed `useNavigate` import (PageHeader handles navigation)

**Lines changed:** 190 insertions, 219 deletions (net -29 lines)

---

### 10.3.2: Migrate Review.new.tsx
**Commit:** `feat(10-3): migrate Review to PageHeader, StatCard, EmptyState, fix cn import`

**Changes:**
- Fixed `cn` import from relative `../lib/utils` to alias `@/lib/utils`
- Replaced `ArrowLeft` icon-only back button with `PageHeader`
- Replaced inline header (title, subtitle, back button) with `PageHeader` component (with `Video` icon, filter actions)
- Replaced 3 inline stat cards with `StatCard` components (Total Segments, Alerts, Detections)
- Replaced inline empty state ("No review segments found") with `EmptyState` component
- Removed `colors` design-tokens import — replaced with Tailwind classes
- Replaced `colors.background.primary` → `bg-background`
- Replaced `colors.background.secondary` → implicit via Card
- Replaced `colors.background.tertiary` → `bg-muted`
- Replaced `colors.text.*` → `text-foreground`, `text-muted-foreground`
- Replaced inline severity color styles with `cn()` + conditional Tailwind classes
- Removed unused `ArrowLeft` from lucide-react import
- Kept `getLabelColor` function (uses local colors for timeline markers)

**Lines changed:** 88 insertions, 111 deletions (net -23 lines)

---

### 10.3.3: Migrate StreamDashboard.new.tsx
**Commit:** `feat(10-3): migrate StreamDashboard to theme-aware styling, clean imports`

**Changes:**
- Replaced `colors.background.primary` inline style with `bg-background` Tailwind class
- Removed `colors` design-tokens import
- Removed unused `Activity` import from lucide-react
- Replaced `colors.interactive.hover` → `hover:bg-white/5`
- Replaced `colors.status.success` → `text-emerald-500`
- Kept custom full-screen header (this is a special streaming page — the gradient overlay and absolute positioning are functional, not theming)
- Kept `z-30`/`z-40` z-index values (matches the 3-tier system: z-30 overlay, z-40 modal from design tokens)
- Preserved all functionality: wake lock, snapshot, mute, fullscreen, mobile action bar

**Lines changed:** 5 insertions, 8 deletions (net -3 lines)

---

## Verification

- **Build:** Passes (`vite build` succeeds in 8.10s)
- **Lint:** No new errors in migrated files
- **Functionality:** All existing features preserved (filters, export, modals, streaming controls)

## Files Modified

| File | Action |
|------|--------|
| `frontend/src/pages/VisitorTimeline.new.tsx` | Migrated to shared components |
| `frontend/src/pages/Review.new.tsx` | Migrated to shared components + fixed cn import |
| `frontend/src/pages/StreamDashboard.new.tsx` | Migrated to theme-aware styling |

## Shared Components Used

| Component | Used In |
|-----------|---------|
| `PageHeader` | VisitorTimeline, Review |
| `StatCard` | VisitorTimeline (4 cards), Review (3 cards) |
| `PageLoading` | VisitorTimeline |
| `EmptyState` | VisitorTimeline, Review |

## Remaining Pages (Plans 10.4)

- BatchDetectionPage.tsx (already uses PageHeader)
- BatchResultsPage.tsx
- Settings.new.tsx (CSS class cleanup)
