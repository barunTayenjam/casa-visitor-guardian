---
phase: 05-pipeline-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/detection/optimizedMotionDetection.ts
  - server/src/detection/consolidatedDetectionService.ts
  - server/src/detection/motionTriggeredDetection.ts
  - server/src/detection/objectDetection.ts
  - server/src/controllers/DetectionController.ts
  - server/src/routes/detectionRoutes.ts
autonomous: true
requirements:
  - CLN-02

must_haves:
  truths:
    - "Legacy Node.js detection modules removed from the codebase"
    - "No remaining imports or references to removed modules in controllers/routes"
    - "TypeScript compiles cleanly after removal"
    - "Detection endpoints continue to function via the Python pipeline"
  artifacts:
    - path: "server/src/detection/"
      provides: "Cleaner detection directory without legacy modules"
      contains: "!(optimizedMotionDetection|consolidatedDetectionService|motionTriggeredDetection|objectDetection)"

key_links:
  - from: "server/src/index.ts"
    to: "server/src/detection/"
    via: "import references"
  - from: "server/src/detection/cleanupService.ts"
    to: "server/src/detection/"
    via: "import references"
---

<objective>
Remove legacy Node.js detection modules that are now redundant after the Python pipeline took over.

These modules predate the Python-native pipeline and relied on HTTP polling
to trigger YOLO inference via Flask. All detection now runs in Python
inline with the RTSP stream, making these files dead code.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
Modules to remove:
- `optimizedMotionDetection.ts` — Node.js MOG2 background subtraction (1103 lines)
- `consolidatedDetectionService.ts` — HTTP client to Python Flask for detection queries
- `motionTriggeredDetection.ts` — triggers YOLO + face recognition on motion events
- `objectDetection.ts` — object detection via HTTP to Flask

These may be imported in:
- `server/src/controllers/DetectionController.ts`
- `server/src/routes/detectionRoutes.ts`
- `server/src/index.ts`
- `server/src/detection/cleanupService.ts`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map all references to legacy detection modules</name>
  <files>server/src/</files>
  <action>
    Search for all imports of the 4 legacy modules across the server codebase.
    Document each file that needs updating.
  </action>
  <verify>
    <automated>rg -l "optimizedMotionDetection\|consolidatedDetectionService\|motionTriggeredDetection\|objectDetection" server/src/</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Remove legacy module files and update imports</name>
  <files>
    server/src/detection/optimizedMotionDetection.ts
    server/src/detection/consolidatedDetectionService.ts
    server/src/detection/motionTriggeredDetection.ts
    server/src/detection/objectDetection.ts
  </files>
  <action>
    Delete the 4 legacy module files. Update all files that imported them to
    either remove the import or redirect to the Python pipeline equivalent.
  </action>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript compiles cleanly</name>
  <files>server/tsconfig.json</files>
  <action>
    Run `npm run build` in the server directory to verify no broken imports.
    Fix any remaining references.
  </action>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node.js → Filesystem | Removing source files is irreversible in git. Ensure all references are cleaned first. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-02 | DoS | Module removal | mitigate | Search all imports before deleting; verify compilation |
</threat_model>

<verification>
1. Files removed: `! ls server/src/detection/optimizedMotionDetection.ts`
2. No remaining imports: `! rg -l "optimizedMotionDetection" server/src/`
3. TypeScript compiles: `cd server && npm run build`
</verification>

<success_criteria>
- [ ] 4 legacy detection module files deleted
- [ ] All imports cleaned up across codebase
- [ ] TypeScript compiles without errors
- [ ] Detection functionality unaffected (runs via Python pipeline)
</success_criteria>
