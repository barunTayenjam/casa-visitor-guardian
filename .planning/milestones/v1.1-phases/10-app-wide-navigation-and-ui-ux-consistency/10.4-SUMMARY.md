# Plan 10.4 Summary: Low-Priority Page Migrations and Cleanup

**Phase:** 10 — App-Wide Navigation and UI/UX Consistency
**Plan:** 10.4
**Date:** 2026-03-27
**Status:** Complete

---

## Tasks Completed

### 10.4.1: Migrate BatchDetectionPage.tsx

**File:** `frontend/src/pages/BatchDetectionPage.tsx`

- Added `PageHeader` import with `@/components/ui/PageHeader`
- Replaced `Button variant="outline"` back button with `PageHeader backTo="/app/streams"`
- Replaced `text-3xl font-bold` title with `PageHeader size="large"`
- Replaced subtitle paragraph with `PageHeader subtitle` prop
- Standardized container from `container mx-auto` to `max-w-7xl mx-auto`

**Changes:** -12 lines removed, +8 lines added (net -4 lines)

### 10.4.2: Migrate BatchResultsPage.tsx

**File:** `frontend/src/pages/BatchResultsPage.tsx`

- Added `PageHeader` and `StatCard` imports
- Added `PageHeader` with back button to `/app/batch-detection` (was missing)
- Replaced `text-3xl font-bold` title with `PageHeader size="large"`
- Replaced 4 inline `Card` stat components with `StatCard` (Total Images, With Detections, Persons Detected, Faces Detected)
- Removed unused `ArrowLeft` import
- Standardized container from `container mx-auto` to `max-w-7xl mx-auto`

**Changes:** -55 lines removed, +32 lines added (net -23 lines)

### 10.4.3: Clean Up Settings Isolated CSS Classes

**File:** `frontend/src/pages/Settings.new.tsx`

Audited three custom CSS classes (`btn-ghost`, `card-surface`, `input-theme`) defined in `frontend/src/index.css`:

| Class | Original | Replacement | Theme-Aware? |
|-------|----------|-------------|-------------|
| `card-surface` | `bg-white/5 border-white/10` (dark-only) | `bg-card border border-border rounded-xl` | Yes |
| `btn-ghost` | `text-foreground/80 hover:bg-white/5` | Standard `variant="ghost"` | Yes |
| `input-theme` | `bg-white/5 border-white/10` (dark-only) | Standard Input component styling | Yes |

**CSS classes retained** in `index.css` because `Login.tsx` still uses `input-theme` (6 occurrences).

**Changes:** -7 lines removed, +7 lines added (net 0 lines — structural cleanup only)

---

## Verification

- All three pages use `PageHeader` consistently
- BatchResultsPage now has a back button (previously missing)
- BatchResultsPage uses `StatCard` for the stat grid
- Settings no longer depends on dark-mode-only custom classes
- All containers standardized to `max-w-7xl`

---

## Files Modified

1. `frontend/src/pages/BatchDetectionPage.tsx`
2. `frontend/src/pages/BatchResultsPage.tsx`
3. `frontend/src/pages/Settings.new.tsx`

## Commits

1. `feat(10-4): migrate BatchDetectionPage to use PageHeader`
2. `feat(10-4): migrate BatchResultsPage to use PageHeader and StatCard`
3. `feat(10-4): clean up Settings page isolated CSS classes`

---

*Summary created: 2026-03-27*
