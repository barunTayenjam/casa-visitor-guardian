# Quick Task 260328-e3h: Batch detection results without bounding boxes - show visitor-relevant classes only

- Updated `BatchDetectionPage` DetectionCard with `DetectionBoxOverlay` component to draw bounding boxes on images via canvas
- Added `bicycle` + `motorcycle` to filter dropdown - show visitor-relevant classes ( person, car, dog, bicycle, motorcycle) as badges with colored border to improve visitor tracking)
- Updated `BatchResultsPage` detail lightbox to show bounding boxes via client-side canvas overlay by default
 show visitor class tags
- Updated `BatchResultsPage` to filter by type dropdown to add bicycle + motorcycle options
- Updated `DetectionCard` in `BatchDetectionPage` to show detection counts by colored badges with confidence %
- </details>

## Plan

**Execution:** Create `DetectionBoxOverlay` component at ` create canvas-based bounding boxes overlay for detection images
- Add `bicycle` + `motorcycle` to filter dropdown to show visitor-relevant classes ( person, car, dog, bicycle, motorcycle) as badges
 colored by to improve visitor tracking
- Updated `BatchResultsPage` detail lightbox shows bounding boxes via client-side canvas overlay by default, show visitor class tags
- Fixed `getImageWithBoxes` to return original image URL ( no-op)
- Updated `DetectionCard` in `BatchDetectionPage` now shows detection counts by colored badges with colored border colors, replacing plain text
- Added `bicycle` + `motorcycle` to filter dropdown
- Show visitor-relevant classes ( person, car, dog, bicycle, motorcycle) as badges with colored border to improve visitor tracking

- Updated `BatchResultsPage` detail lightbox shows bounding boxes via client-side canvas overlay by default ( show visitor class tags
- Updated `BatchResultsPage` to filter by type dropdown, add bicycle + motorcycle options
