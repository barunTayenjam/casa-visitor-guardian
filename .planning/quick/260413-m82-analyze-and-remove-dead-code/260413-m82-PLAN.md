---
phase: quick-analyze-and-remove-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/config/detectionConfig.ts
  - frontend/src/components/dashboard/CameraStream.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Dead code properties removed from detectionConfig.ts"
    - "Dead onFullscreen prop removed from CameraStream.tsx"
  artifacts:
    - path: "server/src/config/detectionConfig.ts"
      provides: "Clean detection config without unused properties"
      contains: "autoDetectObjects, autoDetectFaces, detectionPriority removed"
    - path: "frontend/src/components/dashboard/CameraStream.tsx"
      provides: "CameraStream without undefined onFullscreen prop"
      contains: "onFullscreen={undefined} removed"
  key_links: []
---

<objective>
Remove dead code from detectionConfig.ts and CameraStream.tsx

Purpose: Clean up unused code that adds noise and potential confusion
Output: Clean config and component files
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@server/src/config/detectionConfig.ts
@frontend/src/components/dashboard/CameraStream.tsx

# Analysis findings:
# 1. detectionConfig.ts: autoDetectObjects, autoDetectFaces, detectionPriority are SET in config
#    but NEVER READ in optimizedMotionDetection.ts or anywhere else
# 2. CameraStream.tsx: onFullscreen={undefined} passed to StreamPanel - prop accepted but never wired
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused config properties</name>
  <files>server/src/config/detectionConfig.ts</files>
  <action>Remove unused properties from DetectionConfig interface and both config objects:
- Remove `autoDetectObjects` from interface (line 11) and defaultDetectionConfig (line 35)
- Remove `autoDetectFaces` from interface (line 12) and defaultDetectionConfig (line 36)
- Remove `detectionPriority` from interface (line 13) and defaultDetectionConfig (line 37)
- Remove same 3 properties from lowResourceDetectionConfig (lines 35-37)

These properties are SET but never READ/used in the codebase.</action>
  <verify>grep -r "autoDetectObjects\|autoDetectFaces\|detectionPriority" server/src/detection/ | grep -v "Line [0-9]\+:.*//"</verify>
  <done>DetectionConfig interface has 15 properties (down from 18), both config objects updated</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead onFullscreen prop</name>
  <files>frontend/src/components/dashboard/CameraStream.tsx</files>
  <action>Remove line 470: `onFullscreen={undefined}` - this prop is passed to StreamPanel but is always undefined with no handler wired. The StreamPanel accepts the optional prop (handled internally), but CameraStream never provides a fullscreen function.</action>
  <verify>grep "onFullscreen" frontend/src/components/dashboard/CameraStream.tsx</verify>
  <done>Line containing onFullscreen={undefined} removed from CameraStream.tsx</done>
</task>

</tasks>

<verification>
All unused properties and props removed:
- detectionConfig.ts: 3 unused properties removed
- CameraStream.tsx: dead onFullscreen prop removed
</verification>

<success_criteria>
- [ ] detectionConfig.ts has 15 interface properties (was 18)
- [ ] CameraStream.tsx no longer passes onFullscreen={undefined}
</success_criteria>

<output>
After completion, create `.planning/quick/260413-m82-analyze-and-remove-dead-code/260413-m82-SUMMARY.md`
</output>