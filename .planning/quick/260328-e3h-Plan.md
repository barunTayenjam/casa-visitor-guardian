# Quick Task 260328-e3h: Batch detection results without bounding boxes; focus on visitor-relevant classes

 person, car, dog, bicycle, motorcycle)

- Created `DetectionBoxOverlay` component to draw bounding boxes on images via canvas
- Added `bicycle` + `motorcycle` to filter dropdown - show visitor-relevant classes ( person, car, dog, bicycle, motorcycle) as badges with colored border to improve visitor tracking
- Updated `BatchResultsPage` detail lightbox shows bounding boxes via client-side canvas overlay by default, show visitor class tags
- Updated `DetectionCard` in `BatchDetectionPage` to show detection counts by colored badges
- Fixed `getImageWithBoxes` to return original image URL (no-op)
- Updated `BatchResultsPage` to filter by type dropdown to add bicycle + motorcycle options
</details>

## Plan

**Execution:**
1. Create `DetectionBoxOverlay` component to draw bounding boxes on images via canvas
2. Add `bicycle` + `motorcycle` to filter dropdown to show visitor-relevant classes ( person, car, dog, bicycle, motorcycle) as badges with colored border to improve visitor tracking
- Updated `BatchResultsPage` detail lightbox shows bounding boxes via client-side canvas overlay by default, show visitor class tags
- Updated `BatchResultsPage` to filter by type dropdown to add bicycle + motorcycle options
- Updated `DetectionCard` in `BatchDetectionPage` to show detection counts by colored badges
- Updated `BatchResultsPage` lightbox default shows original image ( with bounding box toggle)
- Fixed `getImageWithBoxes` to return original image URL)
</details>
</plan>
<summary>
</summary>
