---
phase: 06-phase-6-documentation-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - docs/architecture/ADR-001-dual-pipeline-architecture.md
  - docs/architecture/ADR-002-base64-frame-encoding-over-socket-io.md
  - docs/architecture/ADR-004-ffmpeg-subprocess-for-rtsp-ingestion.md
  - docs/architecture/ADR-005-motion-gated-detection-pipeline.md
  - docs/architecture/ADR-006-adaptive-fps-throttling-based-on-viewer-count.md
  - docs/c4-streaming-pipeline.mmd
  - docs/c4-streaming-pipeline.md
autonomous: true
requirements:
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-07
user_setup: []

must_haves:
  truths:
    - "ADR-001 states default mode as 'python-only' not 'legacy'"
    - "ADR-001 no longer describes Node.js FFmpeg subprocess spawning in startStream()"
    - "ADR-002 describes binary frame delivery via Socket.io Buffer, not base64 encoding"
    - "ADR-002 frame relay path references rtspManager.wirePythonWsFrames(), not index.ts handler"
    - "ADR-004 has a revision note flagging stale Node.js FFmpeg spawn sections"
    - "ADR-005 has a revision note flagging stale optimizedMotionDetection.ts references"
    - "ADR-006 line numbers match current rtspManager.ts line positions"
    - "C4 diagrams do not reference OptimizedMotionDetector, Redis cache for detection, FFmpeg-spawning StreamManager, or consolidatedDetectionService HTTP calls"
  artifacts:
    - path: "docs/architecture/ADR-001-dual-pipeline-architecture.md"
      provides: "Corrected default mode + stripped FFmpeg refs"
      contains: "python-only" at the default mode line
    - path: "docs/architecture/ADR-002-base64-frame-encoding-over-socket-io.md"
      provides: "Binary frame delivery documentation"
      contains: "Buffer" and "Socket.io binary mode"
    - path: "docs/architecture/ADR-004-ffmpeg-subprocess-for-rtsp-ingestion.md"
      provides: "Revision note for stale Node.js sections"
      contains: "STALE" or "Revision" in a note block
    - path: "docs/architecture/ADR-005-motion-gated-detection-pipeline.md"
      provides: "Revision note for stale optimizedMotionDetection.ts references"
      contains: "STALE" or "Revision" in a note block
    - path: "docs/architecture/ADR-006-adaptive-fps-throttling-based-on-viewer-count.md"
      provides: "Corrected line references matching current source"
      contains: line numbers that grep confirms exist in rtspManager.ts
    - path: "docs/c4-streaming-pipeline.mmd"
      provides: "Updated C4 diagram with current architecture"
      does-not-contain: "OptimizedMotionDetector"
    - path: "docs/c4-streaming-pipeline.md"
      provides: "Updated C4 diagram with current architecture"
      does-not-contain: "OptimizedMotionDetector"
  key_links:
    - from: "ADR-001 default mode line"
      to: "server/src/config/index.ts:339"
      pattern: "python-only"
    - from: "ADR-002"
      to: "server/src/streams/rtspManager.ts:54-91"
      pattern: "wirePythonWsFrames"
    - from: "ADR-006 line refs"
      to: "server/src/streams/rtspManager.ts (current line numbers)"
      pattern: "grep -n"
    - from: "C4 diagrams"
      to: "rtspManager.ts and index.ts"
      via: "No references to deleted detection modules"
---

<objective>
Refresh all stale architecture documentation: ADR-001 through ADR-006 and both C4 streaming pipeline diagrams. Fix incorrect default mode values, rewrite ADR-002 for binary frame delivery, flag stale sections in ADR-004/ADR-005, correct ADR-006 line numbers, and regenerate C4 diagrams to match the simplified architecture.

**Purpose:** Bugs 2, 3, 4, 5, 7 — every architecture document referenced from the architecture audit has drifted from the live implementation. Documentation that contradicts production code is worse than no documentation (it actively misleads developers and AI agents).

**Output:** Six corrected ADRs and two corrected C4 diagram files.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@server/src/config/index.ts
@server/src/streams/rtspManager.ts
@server/src/index.ts
@server/src/detection/consolidatedDetectionService.ts

**Architecture audit findings for these files (from description sent to planner):**

| Document | Bug | What's Wrong | Fix |
|----------|-----|-------------|-----|
| C4 diagrams (both) | Bug 2 | Reference OptimizedMotionDetector (deleted 05-02), Redis (removed 05-02), StreamManager spawning FFmpeg (removed 05-03), consolidatedDetectionService HTTP calls (stubs) | Regenerate: remove deleted components, update StreamManager role |
| ADR-001 | Bug 3 | Line 111 says `|| 'legacy'` — actual code says `|| 'python-only'`. Also describes FFmpeg spawn behavior in startStream() that no longer exists | Fix default mode, remove FFmpeg spawn references |
| ADR-002 | Bug 4 | Claims frames are `Buffer.toString('base64')` throughout. No base64 encoding occurs in frame relay path anymore — raw Buffer (Socket.io binary mode). Every line reference is stale | Rewrite to describe binary frame delivery |
| ADR-004 | Bug 5 | ~30 lines on Node.js FFmpeg subprocess management at rtspManager.ts lines deleted in 05-03 | Add revision note flagging stale sections |
| ADR-005 | Bug 5 | ~30 lines on optimizedMotionDetection.ts deleted in 05-02 | Add revision note flagging stale references |
| ADR-006 | Bug 7 | All line references (rtspManager.ts:43, :57-59, :125-208, :213-224, :472-506 and StreamController.ts:13) changed after 05-03 rewrite | Update to current line numbers; functional logic still correct |

**Note:** The C4 `.mmd` file and `.md` file should have the same content (rendered differently by GitHub). The `.mmd` has dark theme with `%{init: ...}%` block, the `.md` has a different dark theme. Both need the same structural updates.

**Current rtspManager.ts line numbers (verified):**
- Camera.activeViewers field: line 22 (Set<string>)
- Camera.adaptiveFps field: line 23 (number)
- activeViewers tracking: lines 93-104 (requestStream adds, disconnect deletes)
- getOptimalFps(): search for this method (moved from old :213-224)
- wirePythonWsFrames(): lines 54-91
- Frame emission: lines 75-80, 84-89 (inside wirePythonWsFrames)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ADR-001 default mode + strip Node.js FFmpeg subprocess references</name>
  <files>docs/architecture/ADR-001-dual-pipeline-architecture.md</files>
  <read_first>
    docs/architecture/ADR-001-dual-pipeline-architecture.md (entire file)
    server/src/config/index.ts:339 (actual default mode)
    server/src/config/index.ts:330-341 (current streaming/pipeline config)
  </read_first>
  <action>
    Apply these edits to ADR-001:

    **1. Fix default mode (Bug 3a):**
    - Line 111: Change `mode: (process.env.PIPELINE_MODE as PipelineConfig['mode']) || 'legacy'` to `mode: (process.env.PIPELINE_MODE as PipelineConfig['mode']) || 'python-only'`
    - Line 66: Change table row `| 'legacy' | Node.js owns all RTSP, Python is HTTP inference only |` to `| 'legacy' | DEPRECATED — Node.js owned all RTSP, Python was HTTP inference only |`
    - Line 67: Change `| 'dual' | Default — both pipelines available, per-camera selection |` to `| 'dual' | Both pipelines available, per-camera selection |`
    - Line 123: Change the backward compatibility note from "Existing deployments that don't set PIPELINE_MODE continue operating in legacy mode" to "Existing deployments that don't set PIPELINE_MODE use the Python-native pipeline (python-only)"
    - Add a bolded revision note at the top of the document: `**Revision (2026-05-29):** Default mode changed from 'legacy' to 'python-only' in Phase 5. See CLN-01.`

    **2. Strip Node.js FFmpeg spawn references (Bug 3b):**
    - Line 75-83 (the startStream() quote block): Replace the code block with a note: The `startStream()` method no longer has a pythonEnabled early-return — FFmpeg is never spawned from rtspManager.ts. The full FramePipeline runs in Python.
    - Line 127 (Negative consequences): Remove the bullet point about "Doubled frame relay paths" (lines from index.ts and rtspManager.ts converging) — this is now resolved. The single frame relay path is rtspManager.wirePythonWsFrames().
    - Line 128: Remove bullet about "Duplicated reconnection logic" — Node.js no longer manages FFmpeg subprocess reconnection.
    - Line 129: Remove bullet about "Doubled detection flows" — the legacy pipeline modules were removed in 05-02.
    - Line 130-131: Keep the configuration complexity note but update to remove references to `legacy` mode interactions.
    - Line 135: Remove the risk about "State divergence" — the legacy pipeline modules no longer exist.

    **3. Update consequences section:**
    - Keep Positive bullets, remove any that reference `legacy` mode as active (line 124 "Backward compatibility" should stay but reflect python-only default)
    - Update References section line 165 to remove `rtspManager.ts — Legacy FFmpeg management` and replace with `rtspManager.ts — Socket.io frame relay and viewer tracking`
    - Update line 166 reference to config/index.ts line numbers (was :188-212, update to current lines)

    **4. Remove legacy pipeline diagram:**
    - Lines 16-27: The Legacy Pipeline text diagram at the top now has historical value — wrap it in a `> **Historical reference (pre-v1.3):**` blockquote to indicate it describes code that has been removed.
  </action>
  <verify>
    <automated>
      # Verify no 'legacy' as default mode
      grep "|| 'legacy'" docs/architecture/ADR-001-dual-pipeline-architecture.md && exit 1 || echo "OK: no 'legacy' default"
      # Verify python-only is referenced
      grep "python-only" docs/architecture/ADR-001-dual-pipeline-architecture.md
    </automated>
  </verify>
  <done>
    - ADR-001 default mode matches code: `|| 'python-only'`
    - FFmpeg subprocess spawning references removed or marked historical
    - Consequences section updated to reflect current single-pipeline reality
    - All line references in code blocks match current source code
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ADR-002 for binary frame delivery</name>
  <files>docs/architecture/ADR-002-base64-frame-encoding-over-socket-io.md</files>
  <read_first>
    docs/architecture/ADR-002-base64-frame-encoding-over-socket-io.md (entire file)
    server/src/streams/rtspManager.ts:54-91 (current frame relay — binary Buffer, no base64)
  </read_first>
  <action>
    Rewrite the ADR to describe the current implementation: Socket.io binary mode with native Buffer objects.

    **Title:** Change to "Binary Frame Delivery over Socket.io (Socket.io Binary Mode)"

    **Context section — Replace entirely:**
    Delete the current data path diagram (lines 13-23 showing base64) and replace with:

    ```text
    Python WebSocket Publisher (asyncio WS server :9090)
        ↓ Binary JPEG (no base64)
    PythonWsClient (Node.js, binary WS message handler)
        ↓ Buffer (raw JPEG bytes)
    rtspManager.wirePythonWsFrames()
        ↓
    Socket.io emit ('frame', { cameraId, role, timestamp, data: Buffer })
        ↓ WebSocket transport (binary)
    Frontend Socket.io client
        ↓ Blob URL via URL.createObjectURL(blob)
    React <img /> element
    ```

    **Implementation section:**
    Replace lines 27-49 (the code blocks showing base64) with the actual current code:
    - rtspManager.ts wirePythonWsFrames() at lines 75-80 showing `data` as raw Buffer
    - Remove the index.ts base64 code block entirely (the handler was removed in 06-01)
    - Remove lines 51-58 (the "Why Not Binary WebSocket?" rationale for base64 — no longer relevant)
    - Remove lines 60-72 (frame size characteristics in base64 — update to binary sizes: JPEG ~15-25 KB, no base64 overhead)

    **Decision section:**
    Replace line 78 with: "Use Socket.io binary mode to deliver JPEG frames as native Buffer objects. The frontend constructs Blob URLs with proper memory lifecycle management (revokeObjectURL on replace + unmount). A base64 fallback is preserved for HTTP long-polling transport clients."

    **Consequences — Positive:**
    Rewrite to reflect binary mode benefits:
    - 33% bandwidth reduction over base64
    - No encoding/decoding CPU cost on server or client
    - Single memory copy from PythonWsClient receive to Socket.io emit
    - Blob URL rendering avoids large data URL strings in DOM

    **Consequences — Negative:**
    Keep appropriate items, remove stale ones:
    - Remove "33% size overhead" (no longer base64)
    - Remove "GC pressure from base64 strings"
    - Remove "Double encoding in Python pipeline"
    - Keep scaling concerns, update bandwidth numbers
    - Add note about Blob URL memory lifecycle management requirement

    **Alternatives Considered:**
    Rewrite Alternative 1 (Binary WebSocket Frames) to reflect that this IS the current approach — retitle as "1. Base64 String Encoding (Rejected)" and describe the PREVIOUS approach with reasons it was superseded.

    **References section:**
    Replace all line references:
    - Remove: `server/src/streams/rtspManager.ts:444-513` (FFmpeg stdout parsing, no longer exists)
    - Replace with: `server/src/streams/rtspManager.ts:54-91` (wirePythonWsFrames binary frame relay)
    - Remove: `server/src/index.ts:358-366` (the old base64 index.ts handler, removed in 06-01)
    - Keep: opencv-service reference
    - Keep: frontend reference, update file path if needed

    Add a `> **Revision (2026-05-29):** ADR rewritten to reflect binary frame delivery implemented in Phase 2 (PERF-04). The original base64 encoding approach was fully replaced.` note at the top.
  </action>
  <verify>
    <automated>
      # Verify no base64 encoding claims remain
      grep -c "Buffer.toString('base64')" docs/architecture/ADR-002-base64-frame-encoding-over-socket-io.md || echo "OK: no base64 claims"
      # Verify binary mode is the documented approach
      grep -c "binary" docs/architecture/ADR-002-base64-frame-encoding-over-socket-io.md
    </automated>
  </verify>
  <done>
    - ADR-002 describes binary Buffer delivery, not base64
    - All code blocks match current implementation (no `toString('base64')`)
    - References section points to correct current line numbers
    - Revision note documents the change from base64 to binary mode
  </done>
</task>

<task type="auto">
  <name>Task 3: Flag stale ADR-004/ADR-005 sections + fix ADR-006 line numbers + regenerate C4 diagrams</name>
  <files>
    docs/architecture/ADR-004-ffmpeg-subprocess-for-rtsp-ingestion.md
    docs/architecture/ADR-005-motion-gated-detection-pipeline.md
    docs/architecture/ADR-006-adaptive-fps-throttling-based-on-viewer-count.md
    docs/c4-streaming-pipeline.mmd
    docs/c4-streaming-pipeline.md
  </files>
  <read_first>
    docs/architecture/ADR-004-ffmpeg-subprocess-for-rtsp-ingestion.md (entire file — ~30 lines of Node.js FFmpeg subprocess mgmt)
    docs/architecture/ADR-005-motion-gated-detection-pipeline.md (entire file — ~30 lines on optimizedMotionDetection.ts)
    docs/architecture/ADR-006-adaptive-fps-throttling-based-on-viewer-count.md (entire file — line references)
    server/src/streams/rtspManager.ts (current file for line verification)
    docs/c4-streaming-pipeline.mmd (entire file)
    docs/c4-streaming-pipeline.md (entire file)
  </read_first>
  <action>
    **ADR-004 — Flag stale Node.js sections:**
    The Node.js Side section (lines 14-57) describes FFmpeg subprocess management code that was deleted from rtspManager.ts in 05-03. The code blocks at lines 17-43 and the FrameParsing logic at line 45 no longer exist.

    1. Add a `> **Revision note (2026-05-29):** The Node.js FFmpeg subprocess management sections below describe code deleted in Phase 5 (CLN-03). rtspManager.ts no longer spawns or manages FFmpeg processes — all RTSP ingestion is handled by the Python FFmpegReader. The Node.js section is preserved for historical reference but the code it references has been removed.` blockquote at the top of the document.
    2. Wrap the entire "Node.js Side (Legacy Pipeline)" section (lines 14-57) in a `<details>` block with `<summary>Node.js FFmpeg Subprocess Management (Historical — code removed in v1.3)</summary>`
    3. Update the differences table at lines 94-103 to remove the Node.js row or mark it as historical.
    4. In Consequences > Negative, add a note: "The Node.js FFmpeg management code was removed in Phase 5. The rtspManager no longer manages subprocesses."
    5. In References, update rtspManager.ts line references to point to current lines (wirePythonWsFrames, setup, addCamera) — or remove references to deleted code.

    **ADR-005 — Flag stale optimizedMotionDetection.ts references:**
    The Legacy Pipeline section (lines 44-56) extensively describes OptimizedMotionDetector which was deleted in 05-02.

    1. Add a `> **Revision note (2026-05-29):** The Legacy Pipeline (Node.js) section below describes OptimizedMotionDetector which was deleted in Phase 5 (CLN-02). The motion gate architecture itself is still valid — the Python MotionGate class provides equivalent functionality. The Node.js-specific details are preserved for historical reference.` blockquote at the top.
    2. Wrap the "Legacy Pipeline (Node.js)" section (lines 43-56) in a `<details>` block with `<summary>Legacy Pipeline Motion Detection (Historical — code removed in v1.3)</summary>`
    3. In Consequences, update line 143 (dual implementation drift) to note the legacy implementation has been removed.
    4. In References, remove line 221 (`server/src/detection/optimizedMotionDetection.ts`) and update lines 225-226 if needed.
    5. Update the comparison table at lines 23-28 to remove Node.js column or mark as historical.

    **ADR-006 — Fix stale line references (Bug 7):**
    Using `grep -n` to find current line numbers in rtspManager.ts, update ALL line references:

    The current line mapping (verify with grep before editing):
    - `activeViewers` and `adaptiveFps` fields on Camera interface → lines 22-23
    - Viewer tracking (requestStream/ disconnect) → lines 93-160
    - `getOptimalFps()` method → find its actual current line with `grep -n "getOptimalFps" server/src/streams/rtspManager.ts`
    - Frame emission in wirePythonWsFrames() → lines 75-89
    - StreamController.ts:13 viewer count → verify with `grep -n "viewerCount" server/src/controllers/StreamController.ts`
    - Remove or update line :43 which no longer exists (CameraStream interface was removed)
    - Line :57-59 → update to current activeViewers field location (lines 22-23)
    - Line :125-208 → update to current requestStream/disconnect handler lines
    - Line :213-224 → update to current getOptimalFps line
    - Line :472-506 → update to current frame emission lines in wirePythonWsFrames()
    - Line :147 → update to current viewer count reporting line

    Update all inline code blocks that reference old line numbers — these are scattered throughout the document.

    **C4 Diagrams — Regenerate (Bug 2):**
    Both `.mmd` and `.md` files need the same structural changes (the `.md` file has slightly different theming/formatting but same component model):

    **Streaming Core boundary changes:**
    - StreamManager: Remove "Spawns FFmpeg child process per camera", "Parses MJPEG byte stream → JPEG frames", "FFmpeg reconnect w/ exponential backoff". The description should be: "Socket.io frame relay via PythonWsClient\n• Stores lastFrame per camera\n• Adaptive FPS throttling by viewer count\n• Viewer tracking (socket ID sets)\n• 5-min inactivity timeout"
    - Socket.io Server (index.ts): Remove "Bridges PythonWsClient events → rooms" (this is now done by rtspManager.wirePythonWsFrames()). The description should be: "Handlers: requestStream, stopStream, disconnect\n• Bridges trackingEvent → Socket.io rooms\n• Emits: detection, motionDetected\n         personDetected, faceDetected"
    - Remove the Relationship line `Rel(stream_mgr, ffmpeg_cli, ...)` — StreamManager no longer spawns FFmpeg
    - Remove the Relationship line `Rel(stream_mgr, sio, ...)` — StreamManager no longer emits from FFmpeg frames (it emits from PythonWsClient frames, which is already covered by py_ws → sio relationship)
    - Remove `Rel(stream_mgr, ffmpeg, "FFmpeg subprocess..."` from the second diagram

    **Detection Pipeline boundary changes (both diagrams):**
    - Remove the entire "Detection Pipeline" sub-boundary from the Express Backend
    - Remove `OptimizedMotionDetector` component (optimizedMotionDetection.ts deleted 05-02)
    - Remove `ConsolidatedDetectionService` component (now stubs, no longer part of active detection pipeline)
    - Remove `Redis/In-Memory Cache` from external systems (Redis cache for detection removed 05-02)
    - Remove all relationships referencing motion_det, consolidated, redis, and their cross-boundary edges:
      - `Rel(stream_mgr, motion_det, ...)` — remove
      - `Rel(motion_det, consolidated, ...)` — remove
      - `Rel(consolidated, redis, ...)` — remove
      - `Rel(consolidated, postgres, ...)` — remove
      - `Rel(motion_det, python_svc, ...)` — remove
      - `Rel(consolidated, python_svc, ...)` — remove
      - `Rel(motion_det, ws_pub, ...)` — remove (second diagram)

    **Python container changes (if applicable):**
    - The Python container looks correct (rtsp_ingestion package, FFmpegReader, FramePipeline, etc.)
    - Keep the WebSocketPublisher as-is

    **Relationship link updates:**
    - Change `Rel(py_ws, sio_server, "Bridge events\nframe + detection data", "EventEmitter")` to `Rel(py_ws, sio_server, "Room: forward trackingEvent\n→ Socket.io emit detection, personDetected, motionDetected", "EventEmitter")`

    **Verify the updated diagrams:**
    - No references to `OptimizedMotionDetector` or `optimizedMotionDetection`
    - No references to `ConsolidatedDetectionService` or `consolidatedDetectionService`
    - No references to Redis cache for detection
    - StreamManager description does NOT mention spawning FFmpeg or MJPEG parsing
    - The `.md` version has identical logical structure to the `.mmd` version (only theming differs)
  </action>
  <verify>
    <automated>
      # Verify deleted components not referenced in C4 diagrams
      grep -c "OptimizedMotionDetector" docs/c4-streaming-pipeline.mmd || echo "OK: not found in .mmd"
      grep -c "OptimizedMotionDetector" docs/c4-streaming-pipeline.md || echo "OK: not found in .md"
      grep -c "Spawns FFmpeg" docs/c4-streaming-pipeline.mmd || echo "OK: no FFmpeg spawn in .mmd"
      grep -c "Spawns FFmpeg" docs/c4-streaming-pipeline.md || echo "OK: no FFmpeg spawn in .md"
      # Verify ADR-006 line references match current source
      grep -n "activeViewer" server/src/streams/rtspManager.ts
    </automated>
  </verify>
  <done>
    - ADR-004 has revision note flagging stale Node.js sections
    - ADR-005 has revision note flagging stale optimizedMotionDetection.ts references
    - ADR-006 line references updated to match current rtspManager.ts
    - C4 diagrams (both .mmd and .md) have:
      - Deleted components removed (OptimizedMotionDetector, ConsolidatedDetectionService, Redis cache)
      - StreamManager description updated (no FFmpeg spawn, no MJPEG parsing)
      - FFmpeg external system kept (referenced by Python FFmpegReader)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Developer | Stale docs cause incorrect implementation decisions |
| C4 Diagram → Architecture Reasoning | Outdated diagrams mislead about deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Stale ADR references | mitigate | All stale line numbers and mode values corrected to match current code |
| T-06-05 | Tampering | C4 diagrams | accept | Diagrams match code at time of writing; drift prevention requires ongoing maintenance beyond this phase |
| T-06-06 | Tampering | ADR revision notes | mitigate | Each ADR gets a dated revision note describing what changed and why |
</threat_model>

<verification>
- All 6 ADRs verified against current source code for correctness
- C4 diagrams verified against current architecture (no deleted components)
- Line numbers in ADR-006 confirmed via `grep -n` against current source files
- Each ADR's code snippets match the actual current implementation
</verification>

<success_criteria>
1. ADR-001: Default mode = 'python-only', no FFmpeg spawn refs, consequences updated
2. ADR-002: Describes binary Buffer delivery, no base64 claims, refs point to wirePythonWsFrames()
3. ADR-004: Stale Node.js sections flagged with revision note
4. ADR-005: Stale optimizedMotionDetection.ts references flagged with revision note
5. ADR-006: All line numbers match current source files
6. Both C4 diagrams: No deleted components, StreamManager role updated correctly
</success_criteria>

<output>
Create `.planning/phases/06-phase-6-documentation-audit/06-02-SUMMARY.md` when done
</output>
