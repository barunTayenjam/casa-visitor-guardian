---
phase: 10-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/inMemoryStateService.ts
  - server/src/routes/event-search.ts
autonomous: true
requirements:
  - FIX-001

must_haves:
  truths:
    - "POST /:id/archive deletes the event from PostgreSQL events table"
    - "POST /:id/archive deletes the event image file from disk"
    - "POST /:id/archive removes the event from inMemoryState"
    - "POST /bulk/archive deletes multiple events from DB + disk + memory"
    - "Deletion succeeds even if image file is missing (best-effort file delete)"
    - "Deletion returns 404 if event does not exist in database"
  artifacts:
    - path: "server/src/services/inMemoryStateService.ts"
      provides: "removeEvent(eventId) method on InMemoryStateService"
      contains: "removeEvent"
    - path: "server/src/routes/event-search.ts"
      provides: "Working single + bulk event deletion handlers"
      contains: "DELETE FROM events"
  key_links:
    - from: "server/src/routes/event-search.ts"
      to: "server/src/services/inMemoryStateService.ts"
      via: "inMemoryState.removeEvent(eventId)"
      pattern: "removeEvent"
    - from: "server/src/routes/event-search.ts"
      to: "events table"
      via: "AppDataSource.query DELETE"
      pattern: "DELETE FROM events WHERE id"
---

<objective>
Fix event deletion no-op (FIX-001). The POST /:id/archive handler currently finds the event in inMemoryState but returns success without deleting from DB, disk, or memory. Add real deletion logic for both single and bulk operations.

Purpose: Events cannot currently be deleted — a fundamental data management operation is completely broken.
Output: Working single + bulk event deletion with DB row removal, image file cleanup, and in-memory state sync.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-critical-bug-fixes/10-CONTEXT.md
@.planning/phases/10-critical-bug-fixes/10-RESEARCH.md
</context>

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From server/src/services/inMemoryStateService.ts:
```typescript
// Existing class — add removeEvent() method alongside these existing methods:
export class InMemoryStateService {
  private recentEvents: MotionEvent[] = [];
  addRecentEvent(event: MotionEvent): void { ... }
  getRecentEvents(): MotionEvent[] { ... }
  clearRecentEvents(): void { ... }
  // ADD: removeEvent(eventId: string): boolean
}
```

From server/src/routes/event-search.ts (current no-op handler at line 191-206):
```typescript
router.post('/:id/archive', requireUser, validate({
  params: { id: { type: 'string' as const, required: true, minLength: 1, maxLength: 100 } }
}), (req: Request, res: Response) => {
  // Currently: finds event in inMemoryState, returns success without deleting
});
```

From server/src/database.ts:
```typescript
import { AppDataSource } from '../database.js';
// AppDataSource.query(sql, params) is the parameterized query pattern
```

From server/src/models/Event.ts — events table has `file_path` column for image storage.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add removeEvent() method to InMemoryStateService</name>
  <files>server/src/services/inMemoryStateService.ts</files>
  <read_first>
    - server/src/services/inMemoryStateService.ts (full file — understand existing methods, the recentEvents array, MAX_ENTRIES)
  </read_first>
  <action>
    Add a `removeEvent(eventId: string): boolean` method to the `InMemoryStateService` class, placed after the existing `clearRecentEvents()` method (around line 108). The method finds the event by `id` in the `this.recentEvents` array using `findIndex`, splices it out if found (returning true), or returns false if not found. This method is best-effort — callers should not fail if the event is not in the in-memory buffer (it may have been pushed out by MAX_ENTRIES=100 limit). Do NOT make it async — match the sync pattern of `addRecentEvent` and `clearRecentEvents`.
  </action>
  <acceptance_criteria>
    - `removeEvent` method exists on `InMemoryStateService` class
    - Method signature: `removeEvent(eventId: string): boolean`
    - Method uses `findIndex` to locate event by `id` field, then `splice` to remove it
    - Returns `true` when event was found and removed, `false` when not found
    - `cd server && npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5 && echo "---" && grep -c "removeEvent" server/src/services/inMemoryStateService.ts</automated>
  </verify>
  <done>InMemoryStateService has a removeEvent(eventId) method that returns true/false based on whether the event was in the recentEvents array.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite event archive handler + add bulk delete endpoint</name>
  <files>server/src/routes/event-search.ts</files>
  <read_first>
    - server/src/routes/event-search.ts (full file — current no-op handler at lines 191-206, imports at top)
    - server/src/services/inMemoryStateService.ts (verify removeEvent was added in Task 1)
    - server/src/utils/logger.ts (import pattern for logger)
  </read_first>
  <action>
    Rewrite the `POST /:id/archive` handler (lines 191-206) to perform actual deletion. The new handler must:

    1. Query DB for `file_path` BEFORE deleting the row: `SELECT file_path FROM events WHERE id = $1` with `[eventId]`. If no row returned, return 404 with `{ success: false, error: 'Event not found' }`.
    2. Delete from DB: `DELETE FROM events WHERE id = $1` with `[eventId]`.
    3. Delete image file (best-effort): resolve full path with `path.join(process.cwd(), eventRow.file_path)`, then `fsp.unlink(imagePath)` in a try/catch. Log failure with `logger.warn` but do NOT fail the request.
    4. Remove from inMemoryState: call `inMemoryState.removeEvent(eventId)` — ignore the boolean return (event may not be in memory).
    5. Return `{ success: true, message: 'Event deleted successfully' }`.

    Ensure the required imports exist at the top of the file: `import { promises as fsp } from 'fs'` (if not already present), `import path from 'path'` (if not already present), `import { AppDataSource } from '../database.js'` (if not already present). Check existing imports first — some may already be there.

    Also add a new `POST /bulk/archive` route BEFORE the `/:id/archive` route (Express route ordering matters — `/bulk` must come before `/:id` to avoid `:id` matching "bulk"). This handler:
    - Validates `req.body.eventIds` is an array of strings with at least 1 element
    - For each eventId: query DB for file_path, delete from DB, delete file (best-effort), remove from inMemoryState (best-effort)
    - Return `{ success: true, deleted: <count>, errors: <count of file deletion failures> }`
    - Wrap the entire loop in a try/catch for the DB operations — if the DB delete fails for one event, continue to the next

    Use async `fsp.unlink()` — NOT `fs.unlinkSync()` (which blocks the event loop per RESEARCH.md anti-pattern).
  </action>
  <acceptance_criteria>
    - `POST /:id/archive` handler queries `SELECT file_path FROM events WHERE id = $1` before deletion
    - `POST /:id/archive` handler executes `DELETE FROM events WHERE id = $1`
    - `POST /:id/archive` handler calls `fsp.unlink()` (async, not sync) for image file deletion
    - `POST /:id/archive` handler calls `inMemoryState.removeEvent(eventId)` after DB delete
    - `POST /:id/archive` returns 404 when event not found in DB
    - `POST /bulk/archive` route exists BEFORE `POST /:id/archive` in the router
    - `POST /bulk/archive` accepts `{ eventIds: string[] }` in request body
    - `POST /bulk/archive` iterates eventIds and deletes each from DB + disk + memory
    - No usage of `fs.unlinkSync` anywhere in the file — only `fsp.unlink`
    - `cd server && npx tsc --noEmit` passes with zero errors
    - `grep -c "DELETE FROM events" server/src/routes/event-search.ts` returns at least 2 (single + bulk)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5 && echo "---TS OK---" && grep -c "DELETE FROM events" server/src/routes/event-search.ts && grep -c "fsp.unlink" server/src/routes/event-search.ts && grep -c "removeEvent" server/src/routes/event-search.ts && grep -c "bulk" server/src/routes/event-search.ts</automated>
  </verify>
  <done>Event deletion is fully functional — single event archive deletes from DB + disk + memory, bulk archive handles arrays of event IDs, file deletion failures are logged but don't fail the request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input: event IDs from URL param and request body |
| API→filesystem | Image file paths from DB — trusted source (written by detection pipeline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | event ID param | mitigate | Existing `validate()` middleware enforces `minLength: 1, maxLength: 100` |
| T-10-01-02 | Denial of Service | bulk archive | accept | Bulk delete is authenticated (requireUser) and processes sequentially — no unbounded parallelism |
| T-10-01-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `cd server && npx tsc --noEmit` — zero TypeScript errors
2. `grep -c "removeEvent" server/src/services/inMemoryStateService.ts` — at least 1
3. `grep -c "DELETE FROM events" server/src/routes/event-search.ts` — at least 2 (single + bulk)
4. `grep -c "fsp.unlink" server/src/routes/event-search.ts` — at least 1
5. `grep -c "unlinkSync" server/src/routes/event-search.ts` — exactly 0
6. `grep -c "bulk" server/src/routes/event-search.ts` — at least 1
</verification>

<success_criteria>
- Event deletion removes rows from PostgreSQL `events` table
- Event deletion removes image files from disk (best-effort, logged on failure)
- Event deletion removes events from InMemoryStateService
- Bulk deletion endpoint handles arrays of event IDs
- No synchronous file operations in request handlers
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/10-critical-bug-fixes/10-01-SUMMARY.md` when done
</output>
