---
phase: 09-streaming-ui-simplification
plan: 9.4
subsystem: ui
tags: [react, typescript, z-index, dead-code, state-management, tailwindcss]

# Dependency graph
requires:
  - phase: 08-ui-ux-improvements
    provides: camera streaming UI, adaptive grid, connection overlays
provides:
  - Clean CameraStream with 3-tier z-index layering
  - StreamMetrics interface for consolidated metric state
  - Dead CameraFeed component removed
  - Simplified video feed with name + status dot only
affects: [stream-panel, camera-grid, dashboard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 3-tier z-index convention (z-0 video, z-10 HUD, z-30 modal)
    - StreamMetrics interface for grouped metrics state
    - MotionState interface separate from metrics (frequent updates)

key-files:
  created: []
  modified:
    - frontend/src/components/dashboard/CameraStream.tsx
    - frontend/src/components/live/AdaptiveCameraGrid.tsx
  deleted:
    - frontend/src/components/live/CameraFeed.tsx
    - frontend/src/components/live/CameraFeedControls.tsx
    - frontend/src/components/live/CameraFeedStatus.tsx

key-decisions:
  - "CameraFeed deletion was already committed in 1ebba6c (Plan 9.1) — confirmed dead code with zero imports"
  - "Kept MotionState separate from StreamMetrics — motion updates every 3s while metrics poll every 5s"
  - "Used config.detect for resolution instead of non-existent config.streams — fixed type error from original code"
  - "Status dot shows yellow during connecting/reconnecting (was red before)"

patterns-established:
  - "Z-index tiers: z-0 (video/background), z-10 (HUD/name+dot), z-30 (modal/overlay)"
  - "Metrics state consolidation: group related props into interface objects for passing to child components"

requirements-completed: []

# Metrics
duration: 15min
completed: 2026-03-27
---

# Plan 9.4: Clean Up Dead Code and Z-Index Layering Summary

**Dead CameraFeed removed, z-index consolidated to 3 tiers, metrics state extracted into StreamMetrics interface — CameraStream reduced from 487 to 346 lines**

## Performance

- **Duration:** 15 min
- **Started:** 2026-03-27T09:15:00Z
- **Completed:** 2026-03-27T09:30:00Z
- **Tasks:** 3
- **Files modified:** 2 (CameraStream.tsx, AdaptiveCameraGrid.tsx)

## Accomplishments
- Removed dead CameraFeed.tsx, CameraFeedControls.tsx, CameraFeedStatus.tsx (confirmed zero imports)
- Stripped 5 inline overlay layers from CameraStream: QualityWarning, StreamQualityIndicator, MotionAlertOverlay, inline timestamp, inline controls
- Consolidated z-index to 3 tiers: z-0 (video), z-10 (HUD), z-30 (modal) — removed z-20, z-25 conflicts
- Extracted StreamMetrics interface grouping fps, bandwidth, latency, resolution
- Kept motion state as separate MotionState interface (frequent 3s updates vs 5s metric polls)
- CameraStream reduced from 487 → 346 lines (~29% reduction)
- Added yellow status dot for connecting/reconnecting state (was red before)
- Fixed config.streams → config.detect for resolution (type-correct path)

## Task Commits

1. **Task 9.4.1: Remove Dead CameraFeed Component** — already committed in `1ebba6c` (Plan 9.1)
2. **Task 9.4.2: Consolidate Z-Index Layering** — `1aff26d` (feat)
3. **Task 9.4.3: Extract Overlay State from CameraStream** — `1aff26d` (feat)

**Plan metadata:** pending (this file)

## Files Created/Modified
- `frontend/src/components/dashboard/CameraStream.tsx` — Stripped overlays, fixed z-index, extracted StreamMetrics/MotionState interfaces, 487→346 lines
- `frontend/src/components/live/AdaptiveCameraGrid.tsx` — Changed z-20 to z-10 for exit-focus button, swipe arrows, and camera position indicator

## Decisions Made
- CameraFeed deletion already done in Plan 9.1 commit — no duplicate work needed
- Used config.detect (not config.streams) for resolution — fixed existing type bug
- Status dot color: green (connected), yellow (connecting/reconnecting), red (error/idle)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- camera.config.streams referenced in original code doesn't exist in Camera type — fixed to camera.config.detect

## Next Phase Readiness
- CameraStream is clean with only video + name + status dot — ready for StreamPanel integration
- StreamMetrics interface available for StreamPanel to consume
- MotionState kept separate for real-time updates
- StreamPanel component needs to be wired up in Plan 9.2

---
*Phase: 09-streaming-ui-simplification*
*Completed: 2026-03-27*
