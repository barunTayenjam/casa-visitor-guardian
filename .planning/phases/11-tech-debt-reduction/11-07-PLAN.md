---
phase: 11-tech-debt-reduction
plan: 07
type: execute
wave: 3
depends_on:
  - 11-05
files_modified:
  - server/src/detection/optimizedMotionDetection.test.ts
  - server/src/config/index.test.ts
  - server/src/services/inMemoryStateService.test.ts
  - server/src/utils/logger.test.ts
  - server/tests/setup.ts
autonomous: true
requirements:
  - TD-04
user_setup: []

must_haves:
  truths:
    - "Motion detection utility functions have unit tests with defined input/output"
    - "Configuration loading has tests for valid and invalid camera config"
    - "InMemoryStateService has tests for add/get/cap behavior"
    - "Logger utility has tests for component tagging and log level filtering"
    - "Test suite runs via npm test with passing results"
  artifacts:
    - path: "server/src/detection/optimizedMotionDetection.test.ts"
      provides: "Unit tests for motion detection utility functions"
    - path: "server/src/config/index.test.ts"
      provides: "Tests for config loading, validation, and camera parsing"
    - path: "server/src/services/inMemoryStateService.test.ts"
      provides: "Tests for in-memory state service add/get/cap behavior"
    - path: "server/src/utils/logger.test.ts"
      provides: "Tests for logger component tagging and output"
  key_links:
    - from: "server/src/detection/optimizedMotionDetection.ts"
      to: "server/src/detection/optimizedMotionDetection.test.ts"
      via: "test file imports functions from source"
      pattern: "import.*from.*optimizedMotionDetection"
    - from: "server/jest.config.js"
      to: "server/src/**/*.test.ts"
      via: "Jest test pattern matching"
      pattern: "testMatch|testRegex"
---

<objective>
Establish unit test coverage for core detection pipeline utilities, configuration loading, and critical service paths that currently have zero test coverage.

Purpose: The detection pipeline (3 implementations), stream manager, and config loading have no tests. This makes refactoring risky and regression bugs undetectable. Starting with pure utility functions (no external dependencies) provides the highest test ROI with lowest complexity.
Output: 4 new test files covering motion detection utilities, config loading, in-memory state service, and logger utility.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@server/jest.config.js (Jest configuration)
@server/tests/setup.ts (test setup file)
@server/src/routes/auth.test.ts (existing test pattern to follow)
@server/src/models/User.test.ts (existing model test pattern)
</context>

<interfaces>
<!-- Jest configuration from server/jest.config.js -->
```javascript
// Expected Jest config pattern (from existing server tests)
// Test files: *.test.ts in server/src/ and server/tests/
// Transformer: ts-jest for TypeScript
// Test environment: node
```

<!-- Existing test pattern from auth.test.ts -->
```typescript
// Pattern: describe/it blocks with async handlers
import request from 'supertest';
import { app } from '../index.js';

describe('Auth API', () => {
  it('should register a new user', async () => {
    const res = await request(app).post('/api/auth/register').send({...});
    expect(res.status).toBe(201);
  });
});
```

<!-- Existing test pattern from User.test.ts -->
```typescript
// Pattern: Unit tests for model/entity logic
import { User } from './User.js';

describe('User', () => {
  it('should hash password before save', () => {
    // ...
  });
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Test motion detection utility functions</name>
  <read_first>
    - server/src/detection/optimizedMotionDetection.ts (identify pure utility functions that can be tested without FFmpeg/camera dependencies)
    - server/src/detection/optimizedMotionDetection.ts (look for functions like confidence calculation, zone filtering, cooldown logic, adaptive sensitivity)
    - server/jest.config.js (Jest configuration)
  </read_first>
  <files>server/src/detection/optimizedMotionDetection.test.ts</files>
  <action>
    Create server/src/detection/optimizedMotionDetection.test.ts with unit tests for the pure utility functions in the motion detection module.

    Focus on testable functions that do NOT require FFmpeg, camera streams, or OpenCV service:
    1. **Confidence calculation** — Test the function that calculates motion confidence from contour area. Test cases:
       - Empty frame → confidence 0
       - Small contour (below threshold) → confidence below minimum
       - Large contour (above threshold) → confidence proportional to area
       - Maximum area → confidence capped at maximum (90/100 per CONCERNS.md)

    2. **Zone filtering** — Test the function that checks if a contour falls within a detection zone. Test cases:
       - Contour inside zone → passes filter
       - Contour outside zone → fails filter
       - Contour partially overlapping zone → behavior per implementation

    3. **Cooldown logic** — Test the cooldown period check. Test cases:
       - First detection (no cooldown) → allowed
       - Detection within cooldown period → blocked
       - Detection after cooldown expires → allowed

    4. **Adaptive sensitivity** — Test the time-of-day sensitivity adjustment. Test cases:
       - Daytime (06:00-22:00) → normal sensitivity
       - Nighttime (22:00-06:00) → enhanced sensitivity
       - Edge cases at exact boundary times

    For each test:
    - Import the function directly from optimizedMotionDetection.ts
    - If functions are not exported, export them for testing (add `export` keyword)
    - Use Jest's describe/it/expect pattern
    - Test edge cases and boundary conditions

    If the functions are tightly coupled to the class and cannot be extracted, test the class methods by creating an instance with mock parameters. Do NOT test with real camera frames — use synthetic/mock frame data.

    Target: At least 10 test cases across the 4 categories.
  </action>
  <acceptance_criteria>
    - `ls server/src/detection/optimizedMotionDetection.test.ts` returns file exists
    - File contains at least 4 `describe` blocks (confidence, zones, cooldown, sensitivity)
    - File contains at least 10 `it` test cases total
    - `cd server && npm test -- --testPathPattern="optimizedMotionDetection" --passWithNoTests` runs and passes
    - No test uses real camera frames or FFmpeg (all synthetic/mock data)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- --testPathPattern="optimizedMotionDetection" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Motion detection utility tests created with 10+ test cases across 4 categories, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Test configuration loading and validation</name>
  <read_first>
    - server/src/config/index.ts (full file — identify config loading, validation, camera parsing functions)
    - server/src/config/index.ts (lines 263-302 for camera config loading)
    - server/cameras.json (sample camera configuration)
  </read_first>
  <files>server/src/config/index.test.ts</files>
  <action>
    Create server/src/config/index.test.ts with tests for configuration loading and validation.

    Test categories:

    1. **Environment variable defaults** — Test that config returns correct defaults when env vars are not set:
       - PORT defaults to 9753
       - DB_PORT defaults to 5432
       - NODE_ENV defaults to 'development'
       - TZ defaults to 'Asia/Kolkata'

    2. **Environment variable overrides** — Test that config uses env var values when set:
       - Set PORT=8080 → config.port === 8080
       - Set DB_HOST=myhost → config.dbHost === 'myhost'

    3. **Camera config loading** — Test camera configuration parsing:
       - Valid cameras.json → returns array of camera objects
       - Malformed cameras.json → returns empty array or throws (per current behavior)
       - CAMERAS env var (JSON string) → overrides cameras.json file
       - Legacy format conversion → correctly converts old camera format to new format

    4. **Config validation** — Test validateConfig() function:
       - Valid config → no error thrown
       - Missing JWT secret in production → throws error
       - Missing DB credentials → throws error or warns

    5. **Credential encryption** — Test the encryption/decryption round-trip (if credentialEncryption.ts has testable functions):
       - Encrypt then decrypt → returns original value
       - Different keys → decryption fails

    Use Jest's mocking for environment variables:
    ```typescript
    const originalEnv = process.env;
    beforeEach(() => {
      process.env = { ...originalEnv };
    });
    afterAll(() => {
      process.env = originalEnv;
    });
    ```

    Target: At least 8 test cases across the 5 categories.
  </action>
  <acceptance_criteria>
    - `ls server/src/config/index.test.ts` returns file exists
    - File contains at least 5 `describe` blocks (defaults, overrides, cameras, validation, encryption)
    - File contains at least 8 `it` test cases total
    - `cd server && npm test -- --testPathPattern="config/index" --passWithNoTests` runs and passes
    - Tests do not require a running database or OpenCV service
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- --testPathPattern="config/index" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>Configuration loading tests created with 8+ test cases across 5 categories, all tests pass without external services</done>
</task>

<task type="auto">
  <name>Task 3: Test InMemoryStateService and logger utility</name>
  <read_first>
    - server/src/services/inMemoryStateService.ts (just created in Plan 5)
    - server/src/utils/logger.ts (full file — logging functions, component tagging, config flags)
    - server/src/utils/logger.test.ts (existing test file — check what's already tested)
  </read_first>
  <files>
    server/src/services/inMemoryStateService.test.ts
    server/src/utils/logger.test.ts
  </files>
  <action>
    **InMemoryStateService tests (server/src/services/inMemoryStateService.test.ts):**

    Test the service created in Plan 5:
    1. **addRecentEvent / getRecentEvents** — Test cases:
       - Add one event → getRecentEvents returns array with 1 item
       - Add 101 events → getRecentEvents returns exactly 100 items (cap enforced)
       - Events are returned in reverse chronological order (newest first, due to unshift)

    2. **addAlert / getAlerts** — Test cases:
       - Add one alert → getAlerts returns array with 1 item
       - Add 101 alerts → getAlerts returns exactly 100 items (cap enforced)

    3. **System settings CRUD** — Test cases:
       - Set a setting → get returns the value
       - Set multiple settings → all values retrievable
       - Clear settings → get returns empty object

    4. **Thread safety** — Verify that getRecentEvents returns a copy, not the original array:
       - Get array, modify it → original array in service is unchanged

    **Logger tests (server/src/utils/logger.test.ts):**

    Check if this file already exists and has tests. If it does, add additional tests:
    1. **Component tagging** — Test that logger methods accept and use the source tag:
       - `logger.info('message', 'TestComponent')` — verify tag is included in output

    2. **Log level filtering** — Test that LOGGING_CONFIG flags control output:
       - When info logging is disabled, logger.info() produces no output
       - When error logging is enabled, logger.error() produces output

    3. **Specialized methods** — Test that specialized logger methods work:
       - `logger.motionDetected(cameraId, confidence, timestamp)` — verify correct format
       - `logger.apiRequest(method, url, ip, userAgent)` — verify correct format

    If logger.test.ts does not exist, create it. If it exists, append new tests.

    Target: At least 6 test cases for InMemoryStateService, at least 4 for logger.
  </action>
  <acceptance_criteria>
    - `ls server/src/services/inMemoryStateService.test.ts` returns file exists
    - InMemoryStateService test file has at least 4 `describe` blocks and 6 `it` test cases
    - Logger test file has at least 4 `it` test cases (new or existing)
    - `cd server && npm test -- --testPathPattern="inMemoryStateService" --passWithNoTests` runs and passes
    - `cd server && npm test -- --testPathPattern="logger" --passWithNoTests` runs and passes
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test -- --testPathPattern="inMemoryStateService|logger" --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>InMemoryStateService tests (6+ cases) and logger tests (4+ cases) created and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test isolation | Tests must not depend on external services (DB, OpenCV, cameras) |
| Mock data | Synthetic test data must not contain real credentials or paths |
| Test environment | Jest runs in isolated environment — no cross-test state pollution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18 | Tampering | Test data | mitigate | All test data is synthetic; no real credentials, paths, or camera URLs in test files |
| T-11-19 | Information Disclosure | Test output | accept | Test output goes to stdout; no sensitive data logged in test mode |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- Motion detection utility tests exist with 10+ test cases
- Configuration loading tests exist with 8+ test cases
- InMemoryStateService tests exist with 6+ test cases
- Logger tests exist with 4+ test cases
- All tests pass via npm test
- No tests require external services (database, OpenCV, cameras)
- Test files follow existing Jest patterns from auth.test.ts and User.test.ts
</verification>

<success_criteria>
- 4 new test files created covering detection utilities, config, state service, and logger
- At least 28 total test cases across all new test files
- All tests pass with `npm test`
- Zero external service dependencies in tests
- Test coverage established for previously untested critical paths
</success_criteria>

<output>
Create `.planning/phases/11-tech-debt-reduction/11-07-SUMMARY.md` when done
</output>
