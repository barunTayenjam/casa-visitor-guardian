---
phase: 10-critical-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/auth/index.ts
  - server/src/controllers/AuthController.ts
autonomous: true
requirements:
  - FIX-004
  - FIX-005
  - FIX-006

must_haves:
  truths:
    - "Account is locked after 5 consecutive failed login attempts for 15 minutes"
    - "Successful login resets failedLoginAttempts to 0 and clears lockedUntil"
    - "Locked account returns error message indicating temporary lockout"
    - "MFA setup persists the TOTP secret to users.mfa_secret in the database"
    - "MFA setup does NOT enable MFA immediately — that happens on verify"
    - "MFA verify loads secret from database, not from request body"
    - "MFA verify enables MFA (sets mfa_enabled=true) on first successful verification"
  artifacts:
    - path: "server/src/auth/index.ts"
      provides: "Lockout logic in login() method"
      contains: "failed_login_attempts"
    - path: "server/src/controllers/AuthController.ts"
      provides: "Fixed MFA setup (persists secret) + verify (loads from DB)"
      contains: "mfa_secret"
  key_links:
    - from: "server/src/auth/index.ts"
      to: "users table"
      via: "UPDATE users SET failed_login_attempts, locked_until"
      pattern: "failed_login_attempts"
    - from: "server/src/controllers/AuthController.ts"
      to: "users table"
      via: "SELECT mfa_secret FROM users WHERE id"
      pattern: "mfa_secret"
---

<objective>
Fix account lockout (FIX-004), MFA setup secret persistence (FIX-005), and MFA verify protocol mismatch (FIX-006). Lockout columns exist in DB but are never read/written. MFA setup generates a TOTP secret but never saves it to DB. MFA verify expects the client to send the secret (frontend only sends the code).

Purpose: Authentication security is fundamentally broken — no brute-force protection, MFA enrollment is a no-op, and MFA verification cannot work because the secret was never stored.
Output: Working account lockout after 5 failed attempts, two-step MFA enrollment (setup persists secret, verify enables MFA), and server-side MFA verification.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-critical-bug-fixes/10-CONTEXT.md
@.planning/phases/10-critical-bug-fixes/10-RESEARCH.md
</context>

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From server/src/auth/index.ts — login() method (lines 240-298):
```typescript
// Current SELECT (line 249-254) — MISSING lockout columns:
const result = await AppDataSource.query(
  `SELECT u.id, u.username, u.email, u.password_hash, u.status, r.name as role_name, u.created_at, u.updated_at
   FROM users u LEFT JOIN roles r ON u.role_id = r.id WHERE u.username = $1`,
  [credentials.username]
);
// Lockout check: MISSING between status check (line 263) and password check (line 267)
// Bad password handling: line 269 — just returns error, never increments attempts
// Success handling: line 284 — never resets attempts
```

From server/src/config/index.ts — security config (lines 319-324):
```typescript
security: {
  maxLoginAttempts: parseInt(process.env.MAX_LOGIN_ATTEMPTS || '5', 10),
  lockoutDuration: parseInt(process.env.LOCKOUT_DURATION || '900000', 10), // 15 min in ms
}
```

From server/src/controllers/AuthController.ts — setupMfa (lines 212-234):
```typescript
// Generates secret, returns to client, but NEVER writes to DB
const secret = speakeasy.generateSecret({ name: `SentryVision:...`, length: 20 });
const qrCode = await QRCode.toDataURL(secret.otpauth_url || '');
this.ok(res, { secret: secret.base32, qrCode });
```

From server/src/controllers/AuthController.ts — verifyMfa (lines 236-269):
```typescript
// BUG: extracts { code, secret } from body — frontend only sends { code }
const { code, secret } = req.body;
if (!code || !secret) { // Always fails — secret is undefined
  this.badRequest(res, 'Code and secret are required');
  return;
}
```

From database/migrations/001_create_user_management.sql:
- `failed_login_attempts INTEGER DEFAULT 0` — exists in users table
- `locked_until TIMESTAMP` — exists in users table
- `mfa_secret VARCHAR(32)` — exists in users table (fits 20-byte Base32 = 32 chars)
- `mfa_enabled BOOLEAN DEFAULT FALSE` — exists in users table

From frontend/src/services/api/authService.ts (line 63):
```typescript
const response = await apiClient.post('/auth/mfa/verify', { code }); // Only sends code
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add account lockout logic to login() (FIX-004)</name>
  <files>server/src/auth/index.ts</files>
  <read_first>
    - server/src/auth/index.ts (full file — understand login() at line 240, SELECT query at line 249, status check at line 263, password check at line 267, error return at line 269, success at line 284, imports at top, how config is accessed)
    - server/src/config/index.ts (lines 319-324 — security.maxLoginAttempts and security.lockoutDuration)
  </read_first>
  <action>
    Modify the `login()` method in `server/src/auth/index.ts` to implement account lockout per D-04 from CONTEXT.md. Four changes needed:

    **Change 1 — Add lockout columns to SELECT query (line 249-254):**
    Add `u.failed_login_attempts` and `u.locked_until` to the SELECT clause. The existing query selects `u.id, u.username, u.email, u.password_hash, u.status, r.name as role_name, u.created_at, u.updated_at` — add the two lockout columns after `u.status`.

    **Change 2 — Add lockout check after status check, before password check (between lines 265 and 267):**
    After the `if (dbUser.status !== 'active')` check, add a lockout check:
    - If `dbUser.locked_until` exists and `new Date(dbUser.locked_until) > new Date()`, the account is locked — return `{ success: false, error: 'Account is temporarily locked due to too many failed login attempts. Try again later.' }`.
    - If `locked_until` exists but is in the past, the lockout has expired — proceed with login (the reset on success will clean it up).

    **Change 3 — On bad password (replace line 269-271):**
    Instead of just returning error, first increment failed attempts and conditionally set lockout:
    - `const attempts = (dbUser.failed_login_attempts || 0) + 1`
    - `const lockUntil = attempts >= config.security.maxLoginAttempts ? new Date(Date.now() + config.security.lockoutDuration) : null`
    - Execute: `UPDATE users SET failed_login_attempts = $1, locked_until = $2, updated_at = NOW() WHERE id = $3` with `[attempts, lockUntil, dbUser.id]`
    - Then return the same error: `{ success: false, error: 'Invalid username or password' }`

    **Change 4 — On success, reset lockout counters (after password check passes, before generating token — around line 273):**
    Execute: `UPDATE users SET failed_login_attempts = 0, locked_until = NULL, updated_at = NOW() WHERE id = $1` with `[dbUser.id]`. This is non-blocking — wrap in try/catch and log errors but don't fail the login.

    Access config via `config.security.maxLoginAttempts` and `config.security.lockoutDuration` — `config` is already imported from `'../config/index.js'`.
  </action>
  <acceptance_criteria>
    - SELECT query includes `u.failed_login_attempts` and `u.locked_until` columns
    - Lockout check runs after status check and before password comparison
    - Lockout check compares `new Date(dbUser.locked_until) > new Date()` to determine active lockout
    - On bad password: increments `failed_login_attempts` by 1
    - On bad password: if attempts >= 5 (from config.security.maxLoginAttempts), sets `locked_until` to 15 min from now (from config.security.lockoutDuration)
    - On bad password: executes UPDATE query to persist attempts + lockout to DB
    - On success: executes UPDATE query to reset `failed_login_attempts = 0` and `locked_until = NULL`
    - Success reset is wrapped in try/catch (doesn't fail login on DB error)
    - `cd server && npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5 && echo "---TS OK---" && grep -c "failed_login_attempts" server/src/auth/index.ts && grep -c "locked_until" server/src/auth/index.ts && grep -c "maxLoginAttempts" server/src/auth/index.ts && grep -c "lockoutDuration" server/src/auth/index.ts</automated>
  </verify>
  <done>Login method checks account lockout before password validation, increments failed attempts on bad passwords, locks account after 5 failures for 15 minutes, and resets counters on successful login.</done>
</task>

<task type="auto">
  <name>Task 2: Fix MFA setup persistence + verify protocol (FIX-005, FIX-006)</name>
  <files>server/src/controllers/AuthController.ts</files>
  <read_first>
    - server/src/controllers/AuthController.ts (full file — setupMfa at line 212, verifyMfa at line 236, imports at top including speakeasy and QRCode, how AppDataSource is accessed, auditLogger usage)
    - server/src/auth/index.ts (verify AppDataSource import pattern)
  </read_first>
  <action>
    Fix both MFA methods in `server/src/controllers/AuthController.ts`:

    **Fix setupMfa() — FIX-005 (persist secret to DB):**
    After `speakeasy.generateSecret()` call and before the QR code generation, add a DB update to persist the secret:
    - `await AppDataSource.query('UPDATE users SET mfa_secret = $1, updated_at = NOW() WHERE id = $2', [secret.base32, userId])`
    - Import `AppDataSource` from `'../database.js'` at the top of the file (check if already imported — it may not be since AuthController delegates to authService for DB operations).
    - Do NOT set `mfa_enabled = true` here — that happens on verify (two-step enrollment per CONTEXT.md).
    - Keep the existing response: `{ secret: secret.base32, qrCode }`.

    **Fix verifyMfa() — FIX-006 (load secret from DB, not request body):**
    Rewrite the verifyMfa method completely:
    1. Only extract `code` from `req.body` — remove `secret` extraction.
    2. Validate `code` is present (return badRequest if missing).
    3. Load secret from DB: `const [user] = await AppDataSource.query('SELECT mfa_secret, mfa_enabled FROM users WHERE id = $1', [userId])`.
    4. If no user or `user.mfa_secret` is null, return badRequest with message indicating MFA is not set up.
    5. Verify TOTP: `speakeasy.totp.verify({ secret: user.mfa_secret, encoding: 'base32', token: code, window: 2 })`.
    6. On successful verification: if `user.mfa_enabled` is false (first verification = enrollment completion), set `mfa_enabled = true` via `UPDATE users SET mfa_enabled = true, updated_at = NOW() WHERE id = $1`. Keep the existing audit log call.
    7. On failed verification: return badRequest with "Invalid MFA code".
  </action>
  <acceptance_criteria>
    - `setupMfa()` executes `UPDATE users SET mfa_secret = $1` to persist the Base32 secret
    - `setupMfa()` does NOT set `mfa_enabled = true` (that happens on verify)
    - `setupMfa()` still returns `{ secret: secret.base32, qrCode }` to client
    - `verifyMfa()` extracts only `code` from `req.body` (not `secret`)
    - `verifyMfa()` loads secret from DB via `SELECT mfa_secret, mfa_enabled FROM users WHERE id = $1`
    - `verifyMfa()` returns error if `mfa_secret` is null in DB
    - `verifyMfa()` calls `speakeasy.totp.verify()` with `secret: user.mfa_secret` (from DB, not body)
    - `verifyMfa()` sets `mfa_enabled = true` on first successful verification (when `user.mfa_enabled` was false)
    - `AppDataSource` is imported in AuthController.ts (add if not already present)
    - `grep -c "mfa_secret" server/src/controllers/AuthController.ts` returns at least 3 (persist + load + verify)
    - `cd server && npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>cd server && npx tsc --noEmit 2>&1 | head -5 && echo "---TS OK---" && grep -c "mfa_secret" server/src/controllers/AuthController.ts && grep -c "mfa_enabled" server/src/controllers/AuthController.ts && grep "const { code }" server/src/controllers/AuthController.ts | wc -l</automated>
  </verify>
  <done>MFA setup persists the TOTP secret to users.mfa_secret. MFA verify loads the secret from DB (not request body) and enables MFA on first successful verification. Two-step enrollment flow works: setup→verify→enabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input: login credentials, MFA codes |
| API→DB | User lockout columns, MFA secret storage |
| time source | TOTP verification depends on clock sync between server and client authenticator app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Denial of Service | login lockout | mitigate | Lockout resets after 15 min (config.security.lockoutDuration), max 5 attempts |
| T-10-03-02 | Spoofing | MFA verify with client-sent secret | mitigate | Secret loaded from DB only — never from request body (FIX-006 eliminates this vector) |
| T-10-03-03 | Information Disclosure | MFA secret in response | accept | Setup endpoint returns secret + QR code to authenticated user — this is standard TOTP enrollment |
| T-10-03-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `cd server && npx tsc --noEmit` — zero TypeScript errors
2. `grep -c "failed_login_attempts" server/src/auth/index.ts` — at least 2 (SELECT + UPDATE)
3. `grep -c "locked_until" server/src/auth/index.ts` — at least 3 (SELECT + lockout check + UPDATE)
4. `grep -c "mfa_secret" server/src/controllers/AuthController.ts` — at least 3 (persist + load + verify)
5. `grep "const { code, secret }" server/src/controllers/AuthController.ts` — returns 0 (removed)
6. `grep "const { code }" server/src/controllers/AuthController.ts` — returns at least 1 (only code extracted)
</verification>

<success_criteria>
- Account locks after 5 consecutive failed login attempts for 15 minutes
- Successful login resets failed attempt counter
- MFA setup persists TOTP secret to database
- MFA verify loads secret from database, not request body
- MFA verify enables MFA on first successful verification
- TypeScript compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/10-critical-bug-fixes/10-03-SUMMARY.md` when done
</output>
