---
phase: 09-code-quality-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/index.ts
  - server/src/routes/staticRoutes.ts
  - server/src/services/imageFileService.ts
  - server/src/bootstrap.ts
  - server/src/services/nvidia/types.ts
  - server/src/services/nvidia/prompts.ts
  - server/src/services/nvidia/nvidiaClient.ts
  - server/src/services/nvidia/nvidiaProcessor.ts
  - server/src/services/nvidia/index.ts
  - server/src/services/nvidiaAnalysisService.ts
  - server/src/services/eventSearch/types.ts
  - server/src/services/eventSearchService.ts
  - opencv-service/state.py
  - opencv-service/utils.py
  - opencv-service/pipeline.py
  - opencv-service/routes/detection.py
  - opencv-service/routes/face.py
  - opencv-service/routes/system.py
  - opencv-service/app.py
autonomous: true
requirements: [CQ-01, CQ-02, CQ-03, CQ-04]

must_haves:
  truths:
    - "index.ts is under 100 lines — just imports, app config, middleware, route mounting, server start"
    - "Event and snapshot file serving share a single resolveImage function — no duplicated DB lookup logic"
    - "nvidiaAnalysisService consumers import from the same path and get the same exports"
    - "eventSearchService consumers import from the same path and get the same exports"
    - "opencv-service app.py is a Flask factory under 100 lines — blueprints handle routes"
    - "All decomposed modules use logger.* instead of console.* (per D-09)"
    - "No Socket.io event names, route paths, or HTTP response shapes changed (per D-11)"
  artifacts:
    - path: "server/src/index.ts"
      provides: "Express bootstrap < 100 lines"
      max_lines: 100
    - path: "server/src/services/imageFileService.ts"
      provides: "Deduplicated image cache + path resolution"
      contains: "resolveImage"
    - path: "server/src/routes/staticRoutes.ts"
      provides: "Static file serving + health endpoints"
      contains: "staticRoutes"
    - path: "server/src/bootstrap.ts"
      provides: "Service init + socket relay + graceful shutdown"
      contains: "initializeServices"
    - path: "server/src/services/nvidia/index.ts"
      provides: "Barrel re-export preserving public API"
      contains: "export"
    - path: "server/src/services/nvidia/types.ts"
      provides: "NVIDIA analysis type definitions"
      contains: "AnalysisContext"
    - path: "server/src/services/eventSearch/types.ts"
      provides: "Event search filter interfaces"
      contains: "EventSearchFilters"
    - path: "opencv-service/state.py"
      provides: "Shared Python pipeline state"
      contains: "detector"
    - path: "opencv-service/routes/detection.py"
      provides: "Flask detection blueprint"
      contains: "detection_bp"
    - path: "opencv-service/routes/system.py"
      provides: "Flask system/health blueprint"
      contains: "system_bp"
    - path: "opencv-service/app.py"
      provides: "Flask app factory < 100 lines"
      max_lines: 100
  key_links:
    - from: "server/src/index.ts"
      to: "server/src/routes/staticRoutes.ts"
      via: "import + app.use"
      pattern: "staticRoutes"
    - from: "server/src/routes/staticRoutes.ts"
      to: "server/src/services/imageFileService.ts"
      via: "import + resolveImage call"
      pattern: "resolveImage"
    - from: "server/src/services/nvidiaAnalysisService.ts"
      to: "server/src/services/nvidia/index.ts"
      via: "re-export barrel"
      pattern: "export.*from.*nvidia"
    - from: "opencv-service/app.py"
      to: "opencv-service/routes/detection.py"
      via: "register_blueprint"
      pattern: "register_blueprint"
---

<objective>
Decompose 4 monolithic backend files into focused modules.

Purpose: Reduce cognitive load per file, eliminate duplicated code (CQ-02), and improve maintainability. These 4 files total ~4000 lines — the largest in the codebase.

Output: index.ts < 100L, nvidiaAnalysisService split into 4 modules, eventSearchService types extracted, opencv-service/app.py split into Flask blueprints. All existing consumers continue working unchanged (D-11).
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-phase-9-code-quality-test-coverage/09-CONTEXT.md
@.planning/phases/09-phase-9-code-quality-test-coverage/09-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Decompose index.ts into focused modules (CQ-01, CQ-02, per D-06)</name>
  <files>
    server/src/index.ts,
    server/src/routes/staticRoutes.ts,
    server/src/services/imageFileService.ts,
    server/src/bootstrap.ts
  </files>
  <read_first>
    server/src/index.ts,
    server/src/utils/logger.ts,
    server/src/services/serviceRegistry.ts,
    server/src/routes/index.ts
  </read_first>
  <action>
    Decompose server/src/index.ts (606 lines) into 3 new modules + slim index.ts.

    **1. Create server/src/services/imageFileService.ts** — Extract lines 43–100 (imageCache maps, findImagePath, checkFileExists). Add a new `resolveImage(filename, options)` method that DEDUPLICATES the DB-lookup-then-fallback pattern from the /events and /snapshots handlers (CQ-02). The resolveImage method takes `{ fileTypes: string[], fallbackSubDir: 'events/motion' | 'snapshots' }` and performs: (a) serviceRegistry.getAppDataSource().query() with detection_files lookup using fileTypes filter, (b) if found + file exists, return path, (c) else fall back to findImagePath with fallbackSubDir. Export a singleton `imageFileService` instance. Use `logger.*` instead of `console.*` for all logging. Import logger from '../utils/logger.js'. Import serviceRegistry from '../services/serviceRegistry.js'.

    **2. Create server/src/routes/staticRoutes.ts** — Extract Express Router with:
    - GET /events/:filename handler (currently index.ts lines 114–165). Replace duplicated DB lookup with `imageFileService.resolveImage(filename, { fileTypes: ['event_face', 'event_motion'], fallbackSubDir: 'events/motion' })`.
    - GET /snapshots/:filename handler (currently index.ts lines 168–219). Replace duplicated DB lookup with `imageFileService.resolveImage(filename, { fileTypes: ['snapshot'], fallbackSubDir: 'snapshots' })`.
    - Static middleware mounts (lines 222–248): express.static for /events, /snapshots, /public, dist.
    - Health endpoints (lines 262–345): /health, /health/ready, /api/streams/health.
    Keep all path traversal validation (filename.includes('..') etc.) in the route handlers. Use `logger.*` for all logging. Export the router as `staticRoutes`.

    **3. Create server/src/bootstrap.ts** — Extract:
    - `initializeServices()` function (lines 347–518) — DB init, auth seeding, PythonWsClient setup, Socket.io tracking event relay (lines 384–443), Stream Manager init, Review/Timeline/Detection services, Notification service, Cleanup services. This function takes `io: SocketIOServer` as parameter (the socket relay handler needs the io instance).
    - `gracefulShutdown()` function (lines 531–583) — Takes `signal: string`, `server: http.Server`, `io: SocketIOServer` as parameters.
    - Process signal handlers (SIGTERM, SIGINT) and unhandledRejection — keep in index.ts since they're top-level process events.

    Use `logger.*` for all logging. Import all the same dependencies that index.ts currently imports.

    **4. Slim server/src/index.ts to < 100 lines** — Keep only:
    - Top-level imports (express, http, socket.io, cors, helmet, dotenv, path)
    - Import config, configureRoutes, staticRoutes, initializeServices, gracefulShutdown
    - dotenv.config()
    - Express app creation + middleware (cors, json, helmet)
    - Mount staticRoutes: `app.use(staticRoutes)`
    - HTTP server + Socket.IO server creation
    - Call `await initializeServices(io)` then `configureRoutes(app, io)`
    - `gracefulShutdown` registration with process.on
    - Server listen on PORT
    - Server error handler (EADDRINUSE)

    Remove all image cache variables, findImagePath, checkFileExists, inline route handlers, initializeServices body, gracefulShutdown body. These all move to the new modules.

    **Critical constraint (D-11):** No route path changes, no Socket.io event name changes, no HTTP response shape changes. The /events/:filename and /snapshots/:filename paths stay identical.
  </action>
  <verify>
    <automated>
      bash -c 'LINES=$(wc -l < server/src/index.ts) && echo "index.ts: $LINES lines" && [ "$LINES" -lt 110 ]' &&
      grep -c 'resolveImage' server/src/services/imageFileService.ts &&
      grep -c 'staticRoutes' server/src/routes/staticRoutes.ts &&
      grep -c 'initializeServices' server/src/bootstrap.ts &&
      grep -v '^import' server/src/index.ts | grep -v '^//' | grep -v '^$' | grep -c 'console\.' | grep -q '^0$' || echo "WARN: console.* remaining in index.ts (acceptable for startup messages)"
    </automated>
  </verify>
  <done>
    - index.ts is under 110 lines (target <100, allow small variance)
    - imageFileService.ts exports resolveImage that deduplicates event/snapshot serving (CQ-02)
    - staticRoutes.ts has all 3 image routes + 3 health endpoints
    - bootstrap.ts has initializeServices + gracefulShutdown
    - All new modules use logger.* not console.*
    - Server compiles: cd server && npm run build succeeds
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Decompose nvidiaAnalysisService.ts + eventSearchService.ts (CQ-03, per D-07)</name>
  <files>
    server/src/services/nvidia/types.ts,
    server/src/services/nvidia/prompts.ts,
    server/src/services/nvidia/nvidiaClient.ts,
    server/src/services/nvidia/nvidiaProcessor.ts,
    server/src/services/nvidia/index.ts,
    server/src/services/nvidiaAnalysisService.ts,
    server/src/services/eventSearch/types.ts,
    server/src/services/eventSearchService.ts
  </files>
  <read_first>
    server/src/services/nvidiaAnalysisService.ts,
    server/src/services/eventSearchService.ts,
    server/src/controllers/NvidiaController.ts,
    server/src/utils/logger.ts
  </read_first>
  <action>
    Decompose nvidiaAnalysisService.ts (1063L) into 5 files in a new `server/src/services/nvidia/` directory. Decompose eventSearchService.ts (673L) by extracting types to a separate file.

    **Part A: nvidiaAnalysisService decomposition (per D-07)**

    Create `server/src/services/nvidia/` directory. Extract:

    1. **nvidia/types.ts** — Move all exported interfaces (lines 10–86): AnalysisContext, BoundingBox, PersonDetectionResult, BboxAnalysisResult, NvidianalysisResult, NvidiaApiError. Add `export` to each. Also export DEFAULT_TIMEOUT constant (line 201).

    2. **nvidia/prompts.ts** — Move the 3 prompt constants: SYSTEM_PROMPT (lines 88–114), BBOX_SYSTEM_PROMPT (lines 116–157), PERSON_SYSTEM_PROMPT (lines 159–199). Export all three.

    3. **nvidia/nvidiaClient.ts** — Move normalizeEntityArray (lines 206–232), imageToBase64 (lines 234–244), callNvidiaApi (lines 246–329). These are the HTTP client functions. Import types from './types.js'. Use logger.* for error/warn logging. Export all three functions.

    4. **nvidia/nvidiaProcessor.ts** — Move parseAIResponse (lines 331–429), buildResult (lines 431–467), drawBoundingBoxes (lines 622–703). These are the response processing and drawing functions. Import types from './types.js'. Import callNvidiaApi from './nvidiaClient.js'. Export all three.

    5. **nvidia/index.ts** — Barrel file that re-exports everything consumers need. Import and re-export the 4 public functions: analyzeImage (lines 469–571), checkApiHealth (lines 573–620), analyzeWithBoundingBoxes (lines 705–897), analyzePersons (lines 899–1057). These functions stay in this barrel since they orchestrate client+processor. They import prompts from './prompts.js', client from './nvidiaClient.js', processor from './nvidiaProcessor.js'. Also re-export all types from './types.js'.

    6. **Replace server/src/services/nvidiaAnalysisService.ts** with a one-line re-export barrel:
       ```typescript
       export { analyzeImage, checkApiHealth, analyzeWithBoundingBoxes, analyzePersons } from './nvidia/index.js';
       export type { AnalysisContext, BoundingBox, PersonDetectionResult, BboxAnalysisResult, NvidianalysisResult, NvidiaApiError } from './nvidia/index.js';
       import { analyzeImage, checkApiHealth, analyzeWithBoundingBoxes, analyzePersons } from './nvidia/index.js';
       export default { analyzeImage, checkApiHealth, analyzeWithBoundingBoxes, analyzePersons };
       ```
       This preserves the existing default export and named exports so no consumer imports break (D-11).

    Use `logger.*` instead of `console.*` throughout (replace 15 console.* calls). Import logger from '../../utils/logger.js' (two levels up from nvidia/).

    **Part B: eventSearchService decomposition (per CONTEXT specifics)**

    Create `server/src/services/eventSearch/` directory. Extract:

    1. **eventSearch/types.ts** — Move all 6 exported interfaces (lines 5–71): EventSearchFilters, EventSearchResponse, ListEnhancedFilters, HistoryFilters, LegacySearchFilters, DetectionEventFilters. Export all.

    2. **eventSearch/eventSearchService.ts** — Move the EventSearchService class (lines 73–672). Change the import of interfaces to `'./types.js'`. Keep all methods — this is a lighter decomposition focused on type extraction per the research recommendation (full query builder extraction deferred).

    3. **Replace server/src/services/eventSearchService.ts** with a re-export barrel:
       ```typescript
       export { EventSearchService } from './eventSearch/eventSearchService.js';
       export type { EventSearchFilters, EventSearchResponse, ListEnhancedFilters, HistoryFilters, LegacySearchFilters, DetectionEventFilters } from './eventSearch/types.js';
       import { EventSearchService } from './eventSearch/eventSearchService.js';
       const instance = new EventSearchService();
       export default instance;
       ```
       Wait — the original exports `export default new EventSearchService()`. The re-export must preserve this singleton pattern.

    **Critical constraint (D-11):** NvidiaController.ts imports from `'../services/nvidiaAnalysisService.js'` — this import path must still work. Event search consumers import from `'../services/eventSearchService.js'` — this must still work. No import path changes for consumers.
  </action>
  <verify>
    <automated>
      ls server/src/services/nvidia/types.ts server/src/services/nvidia/prompts.ts server/src/services/nvidia/nvidiaClient.ts server/src/services/nvidia/nvidiaProcessor.ts server/src/services/nvidia/index.ts &&
      ls server/src/services/eventSearch/types.ts &&
      grep -c 'export.*from.*nvidia' server/src/services/nvidiaAnalysisService.ts &&
      grep -c 'export.*from.*eventSearch' server/src/services/eventSearchService.ts &&
      cd server && npm run build
    </automated>
  </verify>
  <done>
    - server/src/services/nvidia/ directory has 5 files (types, prompts, client, processor, barrel)
    - nvidiaAnalysisService.ts is a re-export barrel preserving all named + default exports
    - eventSearch/types.ts has all 6 filter interfaces
    - eventSearchService.ts re-exports the singleton instance
    - Server compiles without errors: cd server && npm run build
    - No consumer import paths changed
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Decompose opencv-service/app.py into Flask blueprints (CQ-04, per D-08)</name>
  <files>
    opencv-service/state.py,
    opencv-service/utils.py,
    opencv-service/pipeline.py,
    opencv-service/routes/detection.py,
    opencv-service/routes/face.py,
    opencv-service/routes/system.py,
    opencv-service/app.py
  </files>
  <read_first>
    opencv-service/app.py
  </read_first>
  <action>
    Decompose opencv-service/app.py (1693 lines) into Flask blueprints + shared modules. The key challenge is managing 6+ module-level globals (_rtsp_service, face_recognition, detector, motion_detector, redis_client, redis_cache, class_names) that are used across routes (Pitfall 1 in research).

    **Step 1: Create opencv-service/state.py** — Shared state module holding initialized instances. Define module-level variables as None defaults:
    ```python
    _rtsp_service = None
    face_recognition = None
    detector = None
    motion_detector = None
    redis_client = None
    redis_cache = None
    class_names = []
    ```
    All blueprints and pipeline.py import from state.py to access/set these globals. This avoids circular imports and NameErrors.

    **Step 2: Create opencv-service/utils.py** — Extract shared helpers:
    - `load_class_names()` function (lines 77–95) — reads coco.names file
    - `draw_detections()` function (lines 1307–1358) — draws bounding boxes on image
    These are pure functions with no dependency on app globals.

    **Step 3: Create opencv-service/routes/detection.py** — Flask Blueprint with detection routes:
    - POST /detect-motion (lines 1124–1184)
    - POST /detect-objects (lines 1186–1228)
    - POST /detect-and-draw (lines 1230–1270)
    - POST /annotate-by-path (lines 1272–1305)
    - POST /recognize-faces (lines 1360–1390)
    - POST /detect-batch (lines 1437–1499)
    - POST /detect-batch-paths (lines 1501–1599)
    Import detector, motion_detector, face_recognition, redis_cache from state module. Import draw_detections from utils. Define `detection_bp = Blueprint('detection', __name__)`.

    **Step 4: Create opencv-service/routes/face.py** — Flask Blueprint with face routes:
    - POST /train-face (lines 1601–1641)
    - POST /retrain-model (lines 1643–1659)
    - GET /known-faces (lines 1661–1693)
    Import face_recognition from state module. Define `face_bp = Blueprint('face', __name__)`.

    **Step 5: Create opencv-service/routes/system.py** — Flask Blueprint with system routes:
    - GET /api/rtsp/metrics (lines 1392–1402)
    - GET /health (lines 1404–1423)
    - GET /status (lines 1425–1435)
    Import _rtsp_service, detector from state module. Define `system_bp = Blueprint('system', __name__)`.

    **Step 6: Create opencv-service/pipeline.py** — Pipeline initialization:
    - Keep DetectionCache class (lines 100–218) here (or import from a separate file if too large)
    - Keep RedisDetectionCache class (lines 228–254) here
    - Keep YOLOObjectDetector class (lines 263–848) here
    - Keep MotionDetector class (lines 850–1112) here
    - `start_rtsp_service()` function (lines 31–75) — initializes RTSPService
    - Module-level initialization code (lines 1114–1122) that creates detector, motion_detector, face_recognition instances and stores them in state.py
    - Also call `state.redis_client = redis_client` and `state.redis_cache = RedisDetectionCache()`
    - The initialization happens at module import time (same as current app.py behavior)
    
    Alternatively, if keeping all 4 classes in pipeline.py makes it too large (>800 lines for the classes alone), keep YOLOObjectDetector and MotionDetector in pipeline.py since they are only used there, and put DetectionCache/RedisDetectionCache in routes/detection.py since they are detection-route-specific.

    **Step 7: Slim opencv-service/app.py to Flask factory < 100 lines** — Keep only:
    - Flask app creation
    - Blueprint registration:
      ```python
      from routes.detection import detection_bp
      from routes.face import face_bp
      from routes.system import system_bp
      app.register_blueprint(detection_bp)
      app.register_blueprint(face_bp)
      app.register_blueprint(system_bp)
      ```
    - Import and trigger pipeline initialization: `import pipeline` (side-effect import initializes detectors)
    - Call `start_rtsp_service()` if needed
    - `if __name__ == '__main__': app.run(...)` block

    **Critical constraint (D-11):** All 13 Flask route paths remain exactly the same. All request/response shapes remain identical. The only change is code organization — routes move from app.py to blueprints.

    **Pitfall avoidance (from research):** All route handlers that reference globals (detector, face_recognition, etc.) must import from state.py, NOT from app.py. Test by starting the Flask app and hitting /health endpoint.
  </action>
  <verify>
    <automated>
      LINES=$(wc -l < opencv-service/app.py) && echo "app.py: $LINES lines" && [ "$LINES" -lt 110 ] &&
      ls opencv-service/state.py opencv-service/utils.py opencv-service/pipeline.py &&
      ls opencv-service/routes/detection.py opencv-service/routes/face.py opencv-service/routes/system.py &&
      grep -c 'state\.' opencv-service/routes/detection.py &&
      grep -c 'register_blueprint' opencv-service/app.py
    </automated>
  </verify>
  <done>
    - opencv-service/app.py is under 110 lines (target <100, factory + blueprint registration)
    - opencv-service/state.py holds shared globals
    - opencv-service/routes/ has 3 blueprint modules (detection, face, system)
    - opencv-service/pipeline.py has detector classes + initialization
    - All 13 Flask routes preserved at same paths
    - Flask app starts: python -c "from app import app; print('OK')" (from opencv-service directory)
    - /health endpoint responds correctly
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → staticRoutes | Untrusted filename in /events/:filename, /snapshots/:filename — path traversal must be validated |
| Python state imports | Blueprint modules import from state.py — incorrect initialization order causes NameError |
| NVIDIA API → backend | External API response parsing must handle malformed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | staticRoutes /events/:filename | mitigate | Preserve existing path traversal validation (filename.includes('..') + regex) in extracted routes |
| T-09-02 | Tampering | staticRoutes /snapshots/:filename | mitigate | Same path traversal validation preserved from original index.ts |
| T-09-03 | Information Disclosure | nvidia/nvidiaClient.ts | accept | Error messages in logging do not contain API keys; existing pattern is safe |
| T-09-SC | Tampering | npm/pip installs | mitigate | No new packages in this plan — only code restructuring. slopcheck not applicable |
</threat_model>

<verification>
1. Line count checks:
   - `wc -l server/src/index.ts` → < 110
   - `wc -l opencv-service/app.py` → < 110
2. File existence:
   - `ls server/src/services/nvidia/types.ts server/src/services/nvidia/prompts.ts server/src/services/nvidia/nvidiaClient.ts server/src/services/nvidia/nvidiaProcessor.ts server/src/services/nvidia/index.ts`
   - `ls server/src/services/eventSearch/types.ts`
   - `ls opencv-service/state.py opencv-service/routes/detection.py opencv-service/routes/system.py`
3. Build verification:
   - `cd server && npm run build` succeeds
4. Import preservation:
   - `grep -r 'from.*nvidiaAnalysisService' server/src/controllers/` still resolves
   - `grep -r 'from.*eventSearchService' server/src/` still resolves
5. Flask health:
   - `cd opencv-service && python -c "from app import app; print('OK')"` succeeds
</verification>

<success_criteria>
- index.ts < 110 lines (target <100)
- app.py < 110 lines (target <100)
- Event/snapshot serving deduplicated via imageFileService.resolveImage (CQ-02)
- nvidiaAnalysisService.ts is a re-export barrel, nvidia/ directory has 5 modules
- eventSearchService.ts has types extracted to eventSearch/types.ts
- Flask blueprints handle all 13 routes at identical paths
- Server compiles: cd server && npm run build
- Flask app imports cleanly: cd opencv-service && python -c "from app import app; print('OK')"
</success_criteria>

<output>
Create `.planning/phases/09-phase-9-code-quality-test-coverage/09-01-SUMMARY.md` when done
</output>
