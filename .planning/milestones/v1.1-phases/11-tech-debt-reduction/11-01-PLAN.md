---
phase: 11-tech-debt-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/StreamDashboard.tsx
  - frontend/src/pages/EventsPage.tsx
  - frontend/src/pages/Settings.tsx
  - frontend/src/pages/Analytics.tsx
  - frontend/src/pages/DayHighlights.tsx
  - frontend/src/App.tsx
  - server/src/models/Session.ts.disabled
  - server/src/models/AuditLog.ts.disabled
  - server/src/models/AuditLog.test.ts
  - server/src/index.ts
  - server/src/routes/index.ts
  - .gitignore
autonomous: true
requirements:
  - TD-05
user_setup: []

must_haves:
  truths:
    - "No .new.tsx files exist in frontend/src/pages/"
    - "No .disabled model files exist in server/src/models/"
    - "server/node-env/ directory is removed and gitignored"
    - "Debug endpoints (/test, /api/cameras/debug) are removed or admin-gated"
    - "Database logging is re-enabled in logger.ts"
  artifacts:
    - path: "frontend/src/pages/StreamDashboard.tsx"
      provides: "Renamed from StreamDashboard.new.tsx"
    - path: "frontend/src/pages/EventsPage.tsx"
      provides: "Renamed from EventsPage.new.tsx"
    - path: "frontend/src/pages/Settings.tsx"
      provides: "Renamed from Settings.new.tsx"
    - path: "frontend/src/pages/Analytics.tsx"
      provides: "Renamed from Analytics.new.tsx"
    - path: "frontend/src/pages/DayHighlights.tsx"
      provides: "Renamed from DayHighlights.new.tsx"
    - path: "frontend/src/App.tsx"
      provides: "Updated imports without .new suffix"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/*.tsx"
      via: "lazy() import"
      pattern: 'lazy\(\(\) => import\("\./pages/[^"]+"\)\)'
---

<objective>
Clean up naming conventions, remove dead artifacts, and re-enable disabled logging. This is the lowest-risk plan — pure renames, deletions, and config flag changes with no behavioral changes.

Purpose: Eliminate confusing naming (.new.tsx, .disabled), remove accidentally committed artifacts (node-env/), remove debug endpoints from production code, and re-enable database logging that was temporarily disabled.
Output: 5 renamed page files, 2 removed disabled files, 1 removed directory, updated App.tsx imports, removed debug endpoints, re-enabled database logging.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename .new.tsx page files and update App.tsx imports</name>
  <read_first>
    - frontend/src/App.tsx (lines 14-18 show current lazy imports with .new suffix)
    - frontend/src/pages/StreamDashboard.new.tsx
    - frontend/src/pages/EventsPage.new.tsx
    - frontend/src/pages/Settings.new.tsx
    - frontend/src/pages/Analytics.new.tsx
    - frontend/src/pages/DayHighlights.new.tsx
  </read_first>
  <files>
    frontend/src/pages/StreamDashboard.tsx
    frontend/src/pages/EventsPage.tsx
    frontend/src/pages/Settings.tsx
    frontend/src/pages/Analytics.tsx
    frontend/src/pages/DayHighlights.tsx
    frontend/src/App.tsx
  </files>
  <action>
    Rename all five .new.tsx files to standard .tsx extension using git mv:
    - `git mv frontend/src/pages/StreamDashboard.new.tsx frontend/src/pages/StreamDashboard.tsx`
    - `git mv frontend/src/pages/EventsPage.new.tsx frontend/src/pages/EventsPage.tsx`
    - `git mv frontend/src/pages/Settings.new.tsx frontend/src/pages/Settings.tsx`
    - `git mv frontend/src/pages/Analytics.new.tsx frontend/src/pages/Analytics.tsx`
    - `git mv frontend/src/pages/DayHighlights.new.tsx frontend/src/pages/DayHighlights.tsx`

    Then update frontend/src/App.tsx lazy imports to remove .new suffix:
    - Line 14: `import("./pages/StreamDashboard.new")` → `import("./pages/StreamDashboard")`
    - Line 15: `import("./pages/EventsPage.new")` → `import("./pages/EventsPage")`
    - Line 16: `import("./pages/Settings.new")` → `import("./pages/Settings")`
    - Line 17: `import("./pages/Analytics.new")` → `import("./pages/Analytics")`
    - Line 18: `import("./pages/DayHighlights.new")` → `import("./pages/DayHighlights")`

    Search for any other imports referencing .new files across the frontend:
    `grep -r "\.new" frontend/src/ --include="*.ts" --include="*.tsx"`
    Update any remaining references found.

    Do NOT change any file contents inside the renamed files — this is purely a rename operation.
  </action>
  <acceptance_criteria>
    - `ls frontend/src/pages/*.new.tsx` returns zero files
    - `ls frontend/src/pages/StreamDashboard.tsx frontend/src/pages/EventsPage.tsx frontend/src/pages/Settings.tsx frontend/src/pages/Analytics.tsx frontend/src/pages/DayHighlights.tsx` all exist
    - `grep -r "\.new" frontend/src/App.tsx` returns no matches
    - `grep -r "\.new" frontend/src/ --include="*.ts" --include="*.tsx"` returns no matches (excluding node_modules)
    - `npm run typecheck` from frontend/ passes with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 page files renamed from .new.tsx to .tsx, all imports updated, TypeScript compilation passes</done>
</task>

<task type="auto">
  <name>Task 2: Remove .disabled model files and clean up related test</name>
  <read_first>
    - server/src/models/Session.ts.disabled
    - server/src/models/AuditLog.ts.disabled
    - server/src/models/AuditLog.ts (active version exists)
    - server/src/models/AuditLog.test.ts
    - server/src/models/index.ts (barrel export file)
    - server/src/models/UserSession.ts (active session model)
  </read_first>
  <files>
    server/src/models/Session.ts.disabled
    server/src/models/AuditLog.ts.disabled
    server/src/models/AuditLog.test.ts
  </files>
  <action>
    Remove the two .disabled model files:
    - `rm server/src/models/Session.ts.disabled` — Session tracking is handled by UserSession.ts (active model). The disabled Session.ts is dead code.
    - `rm server/src/models/AuditLog.ts.disabled` — An active AuditLog.ts already exists (confirmed by ls), making the .disabled version redundant.

    Remove the orphaned test file:
    - `rm server/src/models/AuditLog.test.ts` — This test was for the .disabled model. The active AuditLog.ts should have its own test if needed (out of scope for this plan).

    Verify server/src/models/index.ts does NOT export from the removed files. If it has any import referencing Session.ts.disabled or AuditLog.ts.disabled, remove those lines.

    Do NOT modify the active AuditLog.ts or UserSession.ts files.
  </action>
  <acceptance_criteria>
    - `ls server/src/models/*.disabled` returns zero files
    - `ls server/src/models/AuditLog.test.ts` returns "No such file"
    - `grep -r "\.disabled" server/src/models/index.ts` returns no matches
    - `cd server && npm run build` succeeds (TypeScript compilation)
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Both .disabled model files removed, orphaned test removed, models/index.ts has no references to removed files, backend builds successfully</done>
</task>

<task type="auto">
  <name>Task 3: Remove server/node-env/ directory and add to .gitignore</name>
  <read_first>
    - .gitignore (root level)
    - server/.gitignore (if exists)
  </read_first>
  <files>
    .gitignore
  </files>
  <action>
    Remove the accidentally committed Python virtual environment directory:
    - `rm -rf server/node-env/`

    Add to root .gitignore (append if not already present):
    - Add line: `server/node-env/`
    - Also add common Python virtual env patterns if not present: `**/node-env/`, `**/.venv/`, `**/venv/`

    If server/node-env/ is tracked by git, remove from git index:
    - `git rm -r --cached server/node-env/` (if git tracking exists)

    Do NOT modify any Python requirements.txt or other dependency files.
  </action>
  <acceptance_criteria>
    - `ls server/node-env/` returns "No such file or directory"
    - `grep "node-env" .gitignore` returns at least one match
    - `git ls-files server/node-env/` returns empty (not tracked by git)
  </acceptance_criteria>
  <verify>
    <automated>test ! -d server/node-env/ && grep -q "node-env" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>server/node-env/ directory removed, added to .gitignore, removed from git index if tracked</done>
</task>

<task type="auto">
  <name>Task 4: Remove debug endpoints and re-enable database logging</name>
  <read_first>
    - server/src/index.ts (lines 53-130 for image serving, search for /test endpoint)
    - server/src/routes/index.ts (search for /api/cameras/debug, line ~476)
    - server/src/utils/logger.ts (lines 41-42, 108-111 for database logging)
  </read_first>
  <files>
    server/src/index.ts
    server/src/routes/index.ts
    server/src/utils/logger.ts
  </files>
  <action>
    Remove debug endpoints:
    - In server/src/index.ts: Find and remove the `/test` endpoint (search for `app.get('/test'`). This is a test endpoint with no authentication that should not exist in production code.
    - In server/src/routes/index.ts: Find and remove the `/api/cameras/debug` endpoint (around line 476). This returns detailed camera streaming status without authentication. If any other code references this endpoint, remove those references too.

    Re-enable database logging in server/src/utils/logger.ts:
    - Line 41-42: Change `enableDatabaseLogging = false` to `enableDatabaseLogging = true`
    - Line 108-111: The `writeToDatabase` function currently has an early return `if (!enableDatabaseLogging) return;`. After changing the flag above, this will automatically allow database writes.
    - Do NOT change `enableFileLogging` — leave it as-is per CONCERNS.md (only database logging was disabled "temporarily to fix HTTP hanging").

    Verify no other code depends on the removed debug endpoints by searching:
    - `grep -r "cameras/debug" frontend/src/ server/src/`
    - `grep -r "/test" server/src/index.ts` (ensure only the endpoint definition is removed, not legitimate test references)
  </action>
  <acceptance_criteria>
    - `grep -n "'/test'" server/src/index.ts` returns no matches (debug endpoint removed)
    - `grep -n "cameras/debug" server/src/routes/index.ts` returns no matches (debug endpoint removed)
    - `grep -n "enableDatabaseLogging = true" server/src/utils/logger.ts` returns at least one match
    - `grep -n "enableDatabaseLogging = false" server/src/utils/logger.ts` returns no matches
    - `cd server && npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Debug endpoints removed from index.ts and routes/index.ts, database logging re-enabled in logger.ts, backend builds successfully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File renames | No trust boundary — purely internal file operations |
| Debug endpoint removal | Reduces attack surface by removing unauthenticated information disclosure |
| Database logging re-enable | Writes to PostgreSQL — ensure connection pool handles increased write load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | /test endpoint | mitigate | Remove endpoint entirely — no legitimate use in production |
| T-11-02 | Information Disclosure | /api/cameras/debug | mitigate | Remove endpoint — exposes internal stream state without auth |
| T-11-03 | Tampering | logger.ts config change | accept | Database logging re-enabled; if HTTP hanging recurs, will be caught by monitoring |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- No .new.tsx files remain in frontend/src/pages/
- No .disabled files remain in server/src/models/
- server/node-env/ directory does not exist
- .gitignore contains node-env pattern
- No /test or /api/cameras/debug endpoints in route files
- enableDatabaseLogging = true in logger.ts
- Frontend typecheck passes
- Backend build passes
</verification>

<success_criteria>
- All 5 .new.tsx files renamed to .tsx with imports updated
- Both .disabled model files and orphaned test removed
- server/node-env/ removed and gitignored
- Debug endpoints removed from production code
- Database logging re-enabled
- Zero build errors in both frontend and backend
</success_criteria>

<output>
Create `.planning/phases/11-tech-debt-reduction/11-01-SUMMARY.md` when done
</output>
