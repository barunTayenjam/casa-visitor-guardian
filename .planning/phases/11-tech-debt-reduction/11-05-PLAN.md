---
phase: 11-tech-debt-reduction
plan: 05
type: execute
wave: 3
depends_on:
  - 11-04
files_modified:
  - server/src/routes/cameraRoutes.ts
  - server/src/routes/analyticsRoutes.ts
  - server/src/routes/settingsRoutes.ts
  - server/src/routes/streamRoutes.ts
  - server/src/routes/systemRoutes.ts
  - server/src/routes/index.ts
  - server/src/index.ts
  - server/src/services/inMemoryStateService.ts
autonomous: true
requirements:
  - TD-01
user_setup: []

must_haves:
  truths:
    - "server/src/routes/index.ts is significantly reduced (under 1,500 lines)"
    - "Camera routes extracted to cameraRoutes.ts"
    - "Analytics routes extracted to analyticsRoutes.ts"
    - "Settings routes extracted to settingsRoutes.ts"
    - "Stream control routes extracted to streamRoutes.ts"
    - "System/health routes extracted to systemRoutes.ts"
    - "In-memory state (recentEvents, alerts, cachedSystemSettings) moved to dedicated service"
    - "Inline routes in server/src/index.ts moved to route modules"
  artifacts:
    - path: "server/src/routes/cameraRoutes.ts"
      provides: "Camera listing, control, snapshot, zones, motion settings"
      exports: ["configureCameraRoutes"]
    - path: "server/src/routes/analyticsRoutes.ts"
      provides: "Hourly/weekly analytics, event statistics"
      exports: ["configureAnalyticsRoutes"]
    - path: "server/src/routes/settingsRoutes.ts"
      provides: "System settings CRUD, detection config"
      exports: ["configureSettingsRoutes"]
    - path: "server/src/routes/streamRoutes.ts"
      provides: "Stream start/stop, viewer management"
      exports: ["configureStreamRoutes"]
    - path: "server/src/routes/systemRoutes.ts"
      provides: "Health check, system overview, stats"
      exports: ["configureSystemRoutes"]
    - path: "server/src/services/inMemoryStateService.ts"
      provides: "Typed in-memory state for recentEvents, alerts, cachedSystemSettings"
  key_links:
    - from: "server/src/index.ts"
      to: "server/src/routes/*.ts"
      via: "configure*Routes(app) calls"
      pattern: "configure\w+Routes\(app\)"
    - from: "server/src/routes/index.ts"
      to: "server/src/routes/cameraRoutes.ts"
      via: "import and delegate"
      pattern: "configureCameraRoutes"
---

<objective>
Split the 4,073-line god file server/src/routes/index.ts into domain-specific route modules, extract in-memory state into a dedicated service, and move inline routes from server/src/index.ts into proper route files.

Purpose: The monolithic route file makes any API change require navigating 4,000+ lines, increases merge conflict risk, and prevents isolated testing. Domain-specific modules improve maintainability and enable targeted testing.
Output: 5 new route module files, reduced index.ts, new in-memory state service, cleaned up index.ts entry point.
</objective>

<execution_context>
@/home/barun/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/barun/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@server/src/routes/index.ts (full file — identify route groups by endpoint prefix)
@server/src/routes/visitorRoutes.ts (existing pattern for route extraction)
@server/src/routes/auth.ts (existing pattern for configureAuthRoutes function)
@server/src/index.ts (lines 341-487 for inline routes to extract)
</context>

<interfaces>
<!-- Existing route module pattern from auth.ts -->
```typescript
// Pattern: configuration function that takes Express app
export function configureAuthRoutes(app: Express) {
  app.post('/api/auth/register', authenticate({ roles: ['admin'] }), handler);
  app.post('/api/auth/login', handler);
}
```

<!-- Existing route module pattern from visitorRoutes.ts -->
```typescript
// Pattern: Express Router with default export
const router = Router();
router.get('/api/visitors/list', requireUser, handler);
export default router;
```

<!-- ServiceRegistry interface (from Plan 4) -->
```typescript
import { serviceRegistry } from '../services/serviceRegistry.js';
// Usage: serviceRegistry.getStreamManager(), serviceRegistry.getReviewService(), etc.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract in-memory state into dedicated service</name>
  <read_first>
    - server/src/routes/index.ts (search for "recentEvents", "alerts", "cachedSystemSettings" declarations and all usages)
    - server/src/routes/index.ts (lines 92-93 for in-memory state declarations)
  </read_first>
  <files>server/src/services/inMemoryStateService.ts</files>
  <action>
    Create server/src/services/inMemoryStateService.ts with a class or module that encapsulates the in-memory state currently declared at the top of routes/index.ts.

    The service should manage:
    - `recentEvents`: Array capped at 100 entries, with `add(event)` method that unshifts and pops
    - `alerts`: Array capped at 100 entries, with `add(alert)` method
    - `cachedSystemSettings`: Object/dict for system settings cache, with `get()`, `set()`, `clear()` methods

    Export a singleton instance: `export const inMemoryState = new InMemoryStateService();`

    Provide typed methods:
    - `addRecentEvent(event: Event): void` — adds to array, caps at 100
    - `getRecentEvents(): Event[]` — returns copy of array
    - `addAlert(alert: Alert): void` — adds to array, caps at 100
    - `getAlerts(): Alert[]` — returns copy of array
    - `getSystemSettings(): Record<string, unknown>` — returns cached settings
    - `setSystemSettings(key: string, value: unknown): void` — sets a setting
    - `clearSystemSettings(): void` — clears all settings

    Define proper TypeScript interfaces for Event and Alert types based on the actual data shapes used in routes/index.ts. Do NOT use `any` — infer types from current usage.

    Do NOT modify routes/index.ts in this task — only create the new service file.
  </action>
  <acceptance_criteria>
    - `ls server/src/services/inMemoryStateService.ts` returns file exists
    - File exports `inMemoryState` singleton
    - File has typed methods for recentEvents, alerts, and cachedSystemSettings
    - No `any` types in the service definition
    - `cd server && npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>InMemoryStateService created with typed methods for all three in-memory state objects, backend builds</done>
</task>

<task type="auto">
  <name>Task 2: Extract route groups from index.ts into domain-specific modules</name>
  <read_first>
    - server/src/routes/index.ts (full scan to identify route groups by endpoint prefix)
    - server/src/routes/visitorRoutes.ts (existing extraction pattern to follow)
    - server/src/routes/auth.ts (existing extraction pattern to follow)
    - server/src/routes/storageRoutes.ts (existing Router-based pattern)
  </read_first>
  <files>
    server/src/routes/cameraRoutes.ts
    server/src/routes/analyticsRoutes.ts
    server/src/routes/settingsRoutes.ts
    server/src/routes/streamRoutes.ts
    server/src/routes/systemRoutes.ts
  </files>
  <action>
    Create five new route module files, each with a `configureXxxRoutes(app: Express)` function following the existing pattern from auth.ts. Use `serviceRegistry` for service access (from Plan 4) and `inMemoryState` for in-memory state (from Task 1).

    **cameraRoutes.ts** — Extract all routes matching:
    - `/api/cameras` (listing, details)
    - `/api/cameras/:id/snapshot` (snapshot capture)
    - `/api/cameras/:id/start`, `/api/cameras/:id/stop` (stream control)
    - `/api/cameras/:id/motion` (motion settings)
    - `/api/cameras/:id/zones` (zone management)
    - `/api/cameras/:id/filter` (filter configuration)
    Apply auth middleware per Plan 3 rules (optionalAuth for listing, requireUser for control).

    **analyticsRoutes.ts** — Extract all routes matching:
    - `/api/analytics/hourly`
    - `/api/analytics/weekly`
    - `/api/analytics/*` (any analytics endpoints)
    Replace in-memory `recentEvents` iteration with `inMemoryState.getRecentEvents()`.

    **settingsRoutes.ts** — Extract all routes matching:
    - `/api/settings` (CRUD operations)
    - `/api/detection/config` (GET and PUT — also move from index.ts inline routes)
    Apply requireUser for write operations, optionalAuth for read.

    **streamRoutes.ts** — Extract all routes matching:
    - `/api/streams/*` (stream management endpoints)
    - Any routes that call `streamManager` methods directly
    Use `serviceRegistry.getStreamManager()` for access.

    **systemRoutes.ts** — Extract all routes matching:
    - `/api/health`
    - `/api/stats`
    - `/api/system/overview`
    Use `inMemoryState` and `serviceRegistry` as needed.

    For each file:
    1. Import `Express` from 'express'
    2. Import `serviceRegistry` from '../services/serviceRegistry.js'
    3. Import `inMemoryState` from '../services/inMemoryStateService.js'
    4. Import auth middleware from '../middleware/auth.js'
    5. Export `configureXxxRoutes(app: Express)` function
    6. Copy the route handlers from index.ts, replacing `(global as any)` with `serviceRegistry.get*()` and in-memory state references with `inMemoryState.*()`
    7. Apply auth middleware per Plan 3 rules

    Do NOT modify routes/index.ts yet — only create the new files.
  </action>
  <acceptance_criteria>
    - All 5 new route files exist in server/src/routes/
    - Each file exports a `configure*Routes(app: Express)` function
    - Each file imports `serviceRegistry` and `inMemoryState`
    - No `(global as any)` references in any new route file
    - No direct references to `recentEvents`, `alerts`, or `cachedSystemSettings` variables (use inMemoryState methods)
    - `cd server && npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 domain-specific route modules created with proper imports, auth middleware, and service access patterns, backend builds</done>
</task>

<task type="auto">
  <name>Task 3: Wire extracted routes and remove from god file</name>
  <read_first>
    - server/src/routes/index.ts (current state after Tasks 1-2 — identify what to remove)
    - server/src/index.ts (current route registration pattern)
    - server/src/routes/cameraRoutes.ts (just created)
    - server/src/routes/analyticsRoutes.ts (just created)
    - server/src/routes/settingsRoutes.ts (just created)
    - server/src/routes/streamRoutes.ts (just created)
    - server/src/routes/systemRoutes.ts (just created)
  </read_first>
  <files>
    server/src/routes/index.ts
    server/src/index.ts
  </files>
  <action>
    **Update server/src/index.ts:**
    - Add imports for the new route configuration functions:
      ```
      import { configureCameraRoutes } from './routes/cameraRoutes.js';
      import { configureAnalyticsRoutes } from './routes/analyticsRoutes.js';
      import { configureSettingsRoutes } from './routes/settingsRoutes.js';
      import { configureStreamRoutes } from './routes/streamRoutes.js';
      import { configureSystemRoutes } from './routes/systemRoutes.js';
      ```
    - Register the new routes after existing route registrations:
      ```
      configureCameraRoutes(app);
      configureAnalyticsRoutes(app);
      configureSettingsRoutes(app);
      configureStreamRoutes(app);
      configureSystemRoutes(app);
      ```
    - Remove the inline routes (lines 341-487): GET /api/review, GET /api/review/:id, POST /api/review/:id/acknowledge, GET /api/timeline, GET /api/timeline/active/:camera, GET /api/detection/config, PUT /api/detection/config
    - Move these inline routes into the appropriate new modules:
      - Review/timeline routes → create a new `server/src/routes/reviewTimelineRoutes.ts` OR add to an existing module
      - Detection config routes → settingsRoutes.ts (already included in Task 2)

    **Update server/src/routes/index.ts:**
    - Remove all route handlers that were extracted into the new modules
    - Remove the in-memory state declarations (`recentEvents`, `alerts`, `cachedSystemSettings`)
    - Keep only routes that were NOT extracted (e.g., event CRUD if not moved, or any remaining handlers)
    - Update remaining references to use `inMemoryState` instead of direct variable access
    - The file should be significantly smaller — target under 1,500 lines

    **Move inline review/timeline routes:**
    - Create `server/src/routes/reviewTimelineRoutes.ts` with:
      - GET /api/review (optionalAuth)
      - GET /api/review/:id (optionalAuth)
      - POST /api/review/:id/acknowledge (requireUser)
      - GET /api/timeline (optionalAuth)
      - GET /api/timeline/active/:camera (optionalAuth)
    - Use `serviceRegistry.getReviewService()` and `serviceRegistry.getTimelineService()`
    - Register in index.ts: `import { configureReviewTimelineRoutes } from './routes/reviewTimelineRoutes.js';` and `configureReviewTimelineRoutes(app);`

    After all changes, verify:
    - `wc -l server/src/routes/index.ts` shows significantly reduced line count
    - `cd server && npm run build` succeeds
    - No duplicate route registrations (same path registered in both index.ts and new modules)
  </action>
  <acceptance_criteria>
    - `wc -l server/src/routes/index.ts` returns fewer than 2000 lines (target: under 1500)
    - `grep -n "configureCameraRoutes\|configureAnalyticsRoutes\|configureSettingsRoutes\|configureStreamRoutes\|configureSystemRoutes\|configureReviewTimelineRoutes" server/src/index.ts` returns 6 matches (imports + calls)
    - No inline route handlers remain in server/src/index.ts (no `app.get('/api/review'`, `app.get('/api/timeline'`, etc.)
    - No duplicate route paths across route files
    - `cd server && npm run build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Extracted routes wired into index.ts, inline routes removed from index.ts, god file significantly reduced, no duplicate routes, backend builds</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route extraction | Each new module must independently apply auth middleware — no reliance on global middleware |
| In-memory state service | Thread-safe for Node.js single-threaded model; caps prevent unbounded growth |
| ServiceRegistry access | All new route modules use typed getters (Plan 4) — no (global as any) fallbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14 | Authorization Bypass | Extracted route modules | mitigate | Each module independently applies auth middleware; verify no route is registered without it |
| T-11-15 | Tampering | In-memory state service | accept | Single-threaded Node.js model prevents race conditions; caps at 100 entries |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- routes/index.ts reduced to under 2,000 lines (target: 1,500)
- 6 new route module files created and registered in index.ts
- No inline routes remain in server/src/index.ts
- In-memory state encapsulated in InMemoryStateService
- No (global as any) references in any new route module
- Backend builds successfully
- No duplicate route registrations
</verification>

<success_criteria>
- God file split into 6 domain-specific route modules
- In-memory state moved to dedicated service with typed methods
- All inline routes from index.ts moved to proper route files
- Auth middleware applied consistently across all new modules
- Backend compiles with no errors
- routes/index.ts reduced by at least 50% in line count
</success_criteria>

<output>
Create `.planning/phases/11-tech-debt-reduction/11-05-SUMMARY.md` when done
</output>
